{
  "name": "stellwerk",
  "version": "0.1.0",
  "private": true,
  "description": "Stellwerk – modulare Open-Source Enterprise-AI-Plattform (Monorepo)",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "echo \"(root) no workspaces to lint yet\"",
    "test": "echo \"(root) no workspaces to test yet\""
  }
}
