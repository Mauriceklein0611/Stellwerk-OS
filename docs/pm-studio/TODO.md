---
module: pm-studio
type: doc
status: frozen
imported: 2026-07-08
source: projectmind-os
---

# TODO – Projekt-Hand-off (zwischen Chatfenstern)

> Schlanker Übergabezettel für den Kontext-Reset. **Status-Quelle bleibt
> `docs/task-index.md` + `docs/tasks-archive.md`** – hier nur: zuletzt erledigt /
> als Nächstes / offene Punkte. Pflege gemäß CLAUDE.md → „Token-effizienter Workflow".

## Zuletzt erledigt
- 2026-07-06 · **TASK-065 – Retro-Maßnahmen als Tasks** (Branch `feat/task-065-retro-action-tasks`, PR #113 → `dev`, Task-Datei archiviert). **Schließt den letzten offenen UI-Backlog-Rest** (063/064/066 waren erledigt). Aus einer Retro-**Maßnahme** (String in `SprintRetro.actions`) wird per Klick ein echter **Board-Task**. **Kern-Entscheidung (Abweichung von der Task-Skizze „nur `sourceRetroId`"):** **zwei** additive Felder an `BoardTask` – `sourceRetroId` **und** `sourceRetroAction` (Aktions-Text). Grund: eine Retro hat **mehrere** Aktionen, die Retro-Id allein identifiziert die einzelne Maßnahme nicht ⇒ Basis des Duplikat-Schutzes (je Maßnahme höchstens ein Task). Reine Helfer `src/lib/retro-actions.ts`: `findRetroActionTask`/`retroActionHasTask` (Match nur bei Retro-Id **und** Aktions-Text), `buildRetroActionTask` (Spalte `todo`, Priorität `mittel`, injizierbare Id, leerer Titel ⇒ Fallback auf Aktions-Text). **Store unverändert** – `useBoardStore.addTask` reicht (vergibt via TASK-064 automatisch den Item-Key). UI: in `CeremonyHistory`/`RetroBody` je Maßnahme **„→ Task"-Button** bzw. – sobald ein verknüpfter Task existiert – **Badge „Task erstellt"** (`<StatusBadge>`); neuer vorbefüllter **`RetroActionDialog`** (Titel/Zuständig/Fällig, **keyed-Form** statt setState-in-Effect, Muster `CeremonyDialog`). Orchestrierung in der `/ceremonies`-Seite (Projekt aus dem Sprint der Retro, robust bei gelöschtem Sprint). Zusätzlich: **`SprintDetail`** listet Reviews/Retros des Sprints (Reuse `ceremonyHistory`, Link zu `/ceremonies`, Empty-State). Gelöschter Task bricht nichts (Lookup fällt auf den Button zurück). Keine neue Dependency, keine Persist-Migration (Felder optional). Tests `tests/task-065/` (retro-actions 11, CeremonyHistory-actions 3, RetroActionDialog 3, SprintDetail-ceremonies 3 = 16) + `e2e/ceremonies.spec.ts` (Maßnahme→Task+Dedup+Reload+Board, Sprint-Detail-Listing, 5 gesamt) + `testkonzept.md`. Gate grün (671 Tests, Build) + E2E (Ceremonies 5, Board 12, Sprints 3) + CI.
- 2026-07-06 · **TASK-064 – Lesbare Item-Keys (`PMS-42`)** (Branch `feat/task-064-readable-item-keys`, PR → `dev`, Task-Datei archiviert). Fortlaufende Keys je Projekt für Stories **und** Board-Tasks (eine gemeinsame Laufnummer/Jira-Muster), zusätzlich zur UUID. **Kern-Entscheidung (bewusste Abweichung von der Task-„Betroffene Dateien"-Skizze):** **neuer Leaf-Store `useItemKeyStore`** (`pm-studio-item-keys` v1) mit `prefixes`/`counters`/**zentraler Karte `keys` (itemId→Key)** – **statt** Zähler in `useProjectStore` + `key?`-Feld je Entität. Vorteile: **keine Persist-Migration** auf Backlog-/Board-Store, **keine** Store-Init-Zyklus-Erweiterung (Leaf importiert nichts; Projekt-/Backlog-/Board-Store rufen ihn via `getState()` wie `useActivityStore`), **undo-stabile** Keys (nie aus der Karte entfernt). Reine Helfer `src/lib/item-key.ts`: `derivePrefix(name, taken)` (Initialen mehrerer Wörter bzw. erste 3 Buchstaben bei einem Wort, Akzent-Strip via `normalize`, `PRJ`-Fallback, **deterministischer Zahlensuffix** bei Kollision), `formatItemKey`, `buildKeyBackfill` (pure/idempotent). Vergabe **zentral in den Create-Actions** (`addIdea`→`registerProject`; `addStory`/`importBacklog`/`addTask`→`assignKey`, außerhalb `set`) ⇒ **alle** Anlege-Pfade (Quick-Add, StoryTasks, „In Board übernehmen", Vorschlags-Inbox, Pipeline) bekommen automatisch Keys. **Bestand einmalig nachnummeriert:** `runBackfill` liest die drei Stores **direkt aus `localStorage`** (zyklusfrei, hydrationsreihenfolge-unabhängig, Muster Backlog-Migration), keyt in Erstellungsreihenfolge-Näherung (Stories nach `rank`, dann Tasks nach `column`/`order`), Flag `backfilled`. Anzeige über den Store (**primitiver Selektor `keys[id]`** ⇒ kein getSnapshot-Loop): `TaskCard`, `TaskDialog`-Kopf, `StoryDialog`-Kopf, `BacklogTree`-Zeile (`backlog-key-<id>`, ab sm), `SprintStoryCard` (Row+Card), `BoardListView` erste Spalte **„Key"** (Fallback Kurz-Id; `itemKey` via neuem optionalen `buildBoardRows`-`keys`-Param, auch `/my-work`). **Suche** (`searchEntities`): optionales `keys`-Feld in `SearchSources`, Task/Story matchen zusätzlich per Key, **exakter Key-Match rankt zuerst**, Ergebnis-`badge` (Key) mono in der CommandBar. Keine neue Dependency, keine Persist-Migration bestehender Stores. Tests `tests/task-064/` (item-key 12, useItemKeyStore 8, search-keys 4, BoardListView-key 2 = 24) + `e2e/backlog.spec.ts` (Backfill-Keys + Reload + Suche per Key, 8 gesamt) + `testkonzept.md`; Bestandssuiten unverändert grün (Create-Actions rufen `assignKey` zusätzlich). Gate grün (655 Tests, Build) + E2E (Board 12, Backlog 8, Sprints 3) + CI.
- 2026-07-06 · **TASK-063 – Board-Listenansicht im Octane-Stil (Gruppieren + Aggregat-Fußzeile)** (Branch `feat/task-063-board-list-grouping`, PR #110 → `dev`, Task-Datei archiviert). **Erster UI-Backlog-Quick-Win nach der 056–062-Welle.** Die Board-**Listenansicht** (`BoardListView`) bekommt Gruppieren **und** eine Aggregat-Fußzeile. **Gruppieren:** `SwimlaneSelect` (ARIA-Radiogroup) wird jetzt **auch in der Liste** gerendert (Guard `view === "kanban"` entfernt) und teilt den **persistierten `groupBy`** des Board-Stores mit Kanban; Lanes kommen aus **exakt** `buildSwimlanes` (eine Quelle, TASK-036) → gleiche Reihenfolge, „ohne Zuordnung" zuletzt (`UNGROUPED_LANE`, Label muted), leere Gruppen ausgeblendet. **Fußzeile:** neuer reiner Helfer **`summarizeBoardRows(rows, columns?)`** in `src/lib/board-rows.ts` (Reuse `sumEstimatePt`; „erledigt" **ausschließlich** über `isTerminalColumn` ⇒ konsistent zu TASK-032/038, **keine** zweite Done-Logik) → `{count, plannedPt, donePt}`, gerendert über die neue Presentational **`RowSummary`** als **globale `<tfoot>`-Zeile** (`board-list-footer`) **und** Zwischensumme je Sektionskopf (`board-group-<laneId>`). **⚠️ Architektur-Kern: EINE Tabelle / EIN `useReactTable`** – die Sektionen partitionieren das **bereits sortierte** Row-Model (`table.getRowModel().rows` gefiltert je Lane-Task-Id-Set), **nicht** mehrere Tabellen ⇒ TanStack-Sortierung, Inline-Editing (TASK-030) und Zeilenklick (`TaskDialog`) bleiben **unverändert**; `colSpan` = `table.getVisibleLeafColumns().length`; Gruppen-Sektion via `<Fragment>` (Header-`<tr colSpan>` + gefilterte Daten-`<tr>`). **Abwärtskompatibel:** neue Props `lanes`/`groupBy` sind **optional** mit Default (flache Liste) ⇒ `/my-work` und `tests/task-030` unverändert. Board-Seite reicht das bestehende `lanes`-Memo + `groupBy` durch (view-unabhängig berechnet). Keine neue Dependency, keine Persist-Migration. Tests `tests/task-063/` (`summarize-board-rows` 4: Anzahl/geplant/Leerliste/Custom-Terminal + `BoardListView` 7: Fußzeile/keine Köpfe flat/Sektion+Zwischensumme/ungrouped-zuletzt/alle Zeilen/Inline-Edit+Zeilenklick in Gruppe = 11) + `e2e/board.spec.ts` (Liste gruppieren + Fußzeile + Reload-Persistenz, 12 gesamt) + `testkonzept.md`. Gate grün (631 Tests, Build) + Board-E2E (12) + CI.
- 2026-07-06 · **TASK-062 – Release-Scope (Items einem Release zuordnen + Scope-Filter)** (Branch `feat/task-062-release-scope`, PR → `dev`, Task-Datei archiviert). **Schließt die Milestone-Gruppe „5++++ Backlog als Modul" (056–062) ab.** Stories tragen jetzt einen **Release-Scope** über `UserStory.releaseId` (Jira fixVersion / Octane). **Zuordnen** an zwei Stellen, **eine** Store-Action (`updateStory({ releaseId })`): `StoryDialog` (Release-Select, „Kein Release" = `undefined`) + **inline** in der Backlog-Zeile (`EditableSelectCell` ersetzt den `–`-Platzhalter; **disabled ohne Releases**). Sentinel `NO_RELEASE = "__none__"` (base-ui Select braucht String-Value). **Scope-Filter „Release"** im Backlog-Kopf (nur bei vorhandenen Releases; „Alle/Ohne Release/je Release", UND mit Bestandsfiltern) → **Fußzeile folgt automatisch** (`backlogSummary` über `filteredStories`, keine separate Scope-Summe). Reiner Helfer **`releaseScope(releaseId, stories, tasks, terminalColumns?)`** in `src/lib/release.ts` (n Stories, Σ PT, erledigt **nur** via `isStoryDone`/TASK-038). **`ReleaseCard`:** neue **Scope-Zeile** (release-zugeordnete Stories) **zusätzlich**; alte „Scope:"-Zeile → **„Sprints:"** umbenannt (Content-Scope ≠ Sprint-Fortschritt). **Kaskade** (Muster `detachTag`): neue `useBacklogStore.detachRelease(releaseId)` (setzt `releaseId → undefined`, verwirft keine Story), aufgerufen aus `useReleaseStore.removeRelease`; **kein Import-Zyklus** (Backlog-Store importiert nicht Release-Store); Release-Löschen-**Undo** snapshottet/restauriert jetzt zusätzlich die **Backlog-Slice**. **Abgrenzung (Notiz in `docs/future-scope.md`):** Burnup-Chart & Velocity-Forecast bewusst **nicht**. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-062/` (`release-scope` 5, `detach-release` 2, `BacklogTreeRelease` 3 = 10) + `e2e/backlog.spec.ts` (Inline-Zuordnung + Scope-Filter + Reload-Persistenz, 7 gesamt) + `testkonzept.md`; Bestand `task-041` (`scope`-Prop, „Sprints:") + `task-057` (`releases`-Prop) angepasst. Gate grün (620 Tests, Build) + Backlog-E2E (7).
- 2026-07-06 · **TASK-061 – Projekt-Detail entschlacken + Risiken als Entitäten** (Branch `feat/task-061-project-detail-risks`, PR → `dev`, Task-Datei archiviert). Neuer **`useRiskStore`** (`pm-studio-risks` v1): `risks: Record<projectId, RiskEntry[]>` + `artifactsMigrated`; `setRisks` (Array ersetzen, **funktioniert ohne Pipeline** – nicht mehr no-op), `importRisks` (additiv & idempotent über Risiko-Id, backfillt Status), `removeProjectRisks`, `restore`, `migrateFromArtifacts` (einmalig, Bestand gewinnt). **Zyklusfrei wie `useBacklogStore`** (importiert **nicht** `useProjectStore`, liest Alt-Artefakte in `onRehydrateStorage`/`queueMicrotask` direkt aus `localStorage`). **Pipeline** (`runPipelineForIdea`) ruft nach `importBacklog` zusätzlich `importRisks` (Re-Run ergänzt). **`useProjectStore`:** `removeIdea`-Kaskade → `removeProjectRisks`; **`setRisks` entfernt** (Editieren lebt im Risk-Store), Artefakt-Status-Migration bleibt. **`ProjectDetail`:** Backlog-Tab → neue **`BacklogReferenceCard`** (Rollup Epics/Stories/Σ PT + Deep-Link `/backlog?project=<id>`, **keine zweite Backlog-Render-Logik**); Primäraktion **„Planung starten"** (`/backlog?project=<id>&plan=1`, öffnet Agent-Panel), Pipeline-Batch als **outline**-Sekundäraktion; Risiken-Tab liest/schreibt Risk-Store (RiskTable-API unverändert), **ohne Artefakte nutzbar**; Projekt-Löschen snapshottet zusätzlich die Risk-Slice; Stepper-Klick auf **Backlog-Schritt** → `/backlog?project=`. **`/backlog` page** liest `?plan=1` (`initialPlanFromUrl`) → Panel initial offen. **Konsistenz:** `OverviewTab` „Top-Risiken" + Dashboard-Kachel „Offene Risiken" (`selectMetrics` neuer `risks`-Param) lesen aus dem Risk-Store. **⚠️ zustand-Falle:** Risiken je Projekt als **stabile** Slice `state.risks[id]` selektieren, `?? []` **im Render** (nicht im Selektor → sonst getSnapshot-Loop). **Bewusst offen:** `BacklogView.tsx`/`StoryTasks.tsx` bleiben (weiter getestet), werden im Projekt-Detail nur nicht mehr gerendert. Tests `tests/task-061/` (useRiskStore 14 + BacklogReferenceCard 3) + `testkonzept.md`; Bestand angepasst (009 pipeline promotet Risiken, 011 `selectMetrics`-Param, 045 `setRisks`-Block raus). Gate grün (610 Tests, Build) + Backlog-E2E (6).
- 2026-07-06 · **TASK-060 – Agenten-Panel im Backlog (Mockup): Chat + Vorschlags-Inbox** (Branch `feat/task-060-agent-panel-backlog`, PR → `dev`, Task-Datei archiviert). Rechte **Agenten-Seitenleiste** (`AgentPanel`, Sheet) in `/backlog`: **Chat** (`AgentChat`) mit **genau einer** deterministischen Rückfrage, „Entwurf erstellen" erst nach **expliziter Bestätigung**. **Gates** über neuen additiven `awaiting_review`-Status im `PipelineStepper` (Kopf-Indikator via **`stepStatusesForPhase`** in `pipeline-steps.ts`); ohne „Freigeben" kein Folgeschritt. **Vorschlags-Inbox** (`ProposalCard`): der Scrum-Schritt erzeugt Karten statt Direkt-Import; je Karte **Übernehmen** (→ Backlog-Store `provenance:"agent"`), **Bearbeiten** (→ `StoryDialog` vorbefüllt, `human_edited`), **Verwerfen** + „Alle übernehmen"; **Auto-Modus** („Automatisch durchlaufen") geht ebenfalls durch die Inbox. **Provenance-Badge** an Story-Zeilen (`ProvenanceBadge`/`PROVENANCE_META`, Farbe über `<StatusBadge>`; `human` = kein Badge). Neuer **`useProposalStore`** (leaf, persist light `pm-studio-proposals` v1) hält Chat/Phase/Inbox je Projekt; **Übernahme-Orchestrierung im Panel** (Ränge aus aktuellem Backlog, kein Store-Zyklus – Muster TASK-043). **Typen** `AgentConversation`/`AgentMessage` aus `docs/agent-system.md`. **⚠️ Architektur-Seam:** Mock-Content-Funktionen in neuem **`src/lib/mock-planning.ts`** (nicht in `mock-agent-service.ts`), sonst bricht der Guard `tests/task-009/architecture.test.ts` (nur `agent-service.ts` darf die Mock-Impl importieren); der Leaf-Store darf `agent-service.ts` wegen Zyklus ohnehin nicht importieren. **⚠️ Scope-Entscheidung (Nutzer): „Nur Agent-Panel"** – `runPipelineForIdea` (ProjectDetail „Pipeline ausführen") bleibt unverändert (Legacy-Direkt-Import), das AC „kein Mock-Pfad schreibt ungefragt" gilt fürs Panel; hält Bestandstest `tests/task-009/pipeline-store.test.ts`. Keine neue Dependency, keine Persist-Migration bestehender Stores. Tests `tests/task-060/` (proposals/provenance/pipeline-steps/store + `AgentPanel`-Inbox-Flows, 30) + `e2e/backlog.spec.ts` (Auto→Inbox→„Alle übernehmen", Beweis „vorher leer", 6 gesamt) + `testkonzept.md`. Gate grün (597 Tests, Build) + Backlog-E2E (6).
- 2026-07-03 · **TASK-066 – UI-Sprache konsequent deutsch** (Branch `feat/task-066-ui-language-de`, PR → `dev`, Task-Datei archiviert). Verbliebene englische UI-Chrome-Strings übersetzt (eigene NFR erfüllt). **`src/lib/navigation.ts`** (einzige Quelle für Sidebar **und** CommandBar): Gruppen `Overview→Übersicht`, `Projects→Projekte`, `Agents→Agenten`, `Delivery→Umsetzung` (System bleibt); Items `Projects→Projekte`, `New Idea→Neue Idee`, `Agents→Agenten`. **Generische Labels ohne Fachbegriff-Status:** `Assignee→Zuständig` (`src/lib/swimlanes.ts` `SWIMLANE_MODES` + `BoardListView`-Spalte/aria-label), `Owner→Verantwortlich` (`RiskTable`-Spalte + `RiskDialog`), sr-only `Close→Schließen` (`ui/dialog.tsx`, `ui/sheet.tsx`). **Bewusst unübersetzt** (Scrum-/Kanban-Anglizismen, Glossar in `docs/design-system.md`): Backlog, Board, Sprint(s), Release(s), Epic, Story, Task, Ceremony/Ceremonies, Velocity, Workflow, Dashboard, Team. **Entscheidung dokumentiert:** PRD-NFR (`docs/product-requirements.md`) von „UI englisch" auf **durchgängig deutsch**. **Keine i18n-Bibliothek**, keine neue Dependency, keine Persist-Migration. Tests `tests/task-066/navigation-language.test.ts` (5, prüft Datenquelle `navGroups`/`navItems` – kein next/navigation-Mock im Projekt) + `testkonzept.md`; Bestandstests an neue Labels **angepasst** (`tests/task-036/SwimlaneSelect`, `e2e/board.spec.ts`). Gate grün (567 Tests, Build) + Board-E2E (11).
- 2026-07-03 · **TASK-059 – Sprint-Planung v2 (vertikales Layout mit klickbaren Items)** (PR → `dev`, Task-Datei archiviert). Die `/sprints`-Übersicht vom horizontalen Spalten-Scroll (`SprintColumn`) auf **vertikal gestapelte, ein-/ausklappbare `SprintSection`-Abschnitte** (Jira/Azure-Muster) umgebaut; Backlog-Sektion **zuletzt**. Neu **`src/components/sprint/SprintSection.tsx`**: Kopf (Collapse-Toggle, Name, Status-/Aktiv-Badge, Zeitraum, Ziel, Fortschrittsbalken, PT-Summe, Öffnen-Link `/sprints/[id]`, Review-/Edit-Aktionen) über **droppable** Story-Zeilenliste + **eigen-einklappbarem** Auslastungs-Block. **`SprintStoryCard`** neue **`variant="row"`** (einzeilig, kompakt); Griff-vs-Klick aus TASK-058 bleibt (Griff zieht, Zeilenkörper öffnet `StoryDialog`). **Commitment-Warnung:** neuer reiner Helfer **`sprintCommitment(plannedPt, persons)`** in `src/lib/capacity.ts` (Σ Team-Kapazität; **nie** Warnen ohne Kapazität) → Sektionskopf `<StatusBadge status="warning" label="Überplant">` bei Überplanung. **D&D unverändert** (`assignStory`, bestehende Sensorik/`data`), Fortschritt/Done nur über `sprintProgress`/`isStoryDone` (TASK-038). **`SprintColumn.tsx` entfernt** (nur hier genutzt); **`.thin-scrollbar`-Utility (TASK-053) aus `globals.css` entfernt** (entfällt mit dem Layout). `today` erst nach `useHydrated`-Gate; keine neue Dependency, keine Persist-Migration. Tests `tests/task-059/` (`sprint-commitment` 5, `SprintSection` 7, `SprintStoryCard` Row 2 = 14) + `testkonzept.md`; `e2e/sprints.spec.ts` auf `sprint-section-*` umgestellt. Gate grün (562 Tests, Build) + Sprint-E2E (3). **⚠️ Test-Isolationsfalle:** `userEvent.click` auf den dnd-Griff **startet** einen dnd-kit-Drag (Default-Sensor aktiviert auf `pointerdown`), dessen jsdom-Reststate den **direkt folgenden** Test stört ⇒ Griff-Klicks in Unit-Tests via `fireEvent.click` (kein Pointer-Sequence). **⚠️ Bekannt/out of scope:** `e2e/my-work.spec.ts` scheitert weiter am veralteten Projekt-Seed (Version 1 ohne `risks`); CI führt kein E2E aus.
- 2026-07-03 · **TASK-058 – Story-Dialog / Item-Detail (eine Komponente für überall)** (PR #103 → `dev`, Task-Datei archiviert). Ein wiederverwendbarer **`StoryDialog`** (Muster `TaskDialog`), der eine User Story **überall** öffnet/editiert: **Backlog** (Zeilenklick), **Sprint-Planung** (Kartenkörper-Klick), **Sprint-Detail** (Story-Kopf). **Typwechsel:** `UserStory.acceptance_criteria` von `string[]` → **`AcceptanceCriterion[] { id, text, done }`** (abhakbar); das **Agenten-Artefakt `BacklogStory` bleibt `string[]`** (Run-Snapshot), `flattenBacklog` mappt via `criteriaFromStrings`. Neue reine Transforms **`src/lib/acceptance.ts`** (Spiegel `checklist.ts`, aber Story-Ebene + **Pflichtfeld** ⇒ `normalizeAcceptance` gibt **immer** ein Array). **`ActivityEntityType` additiv um `"story"`**. **Store (`useBacklogStore`):** neue Action **`moveStoryToEpic`** (setzt `epicId`, rankt ans **Ende** des Ziel-Epics, hebt `agent`→`human_edited`; No-op bei gleichem/unbekanntem Epic); **Activity-Logging** (`logActivity`, Leaf außerhalb `set`) für **create/update/delete/move** – **`reorderStory` loggt bewusst NICHT** (Granularität TASK-043). **Persist-Migration v1→v2** (exportiert als **`migrateBacklogPersistedState`**, deshalb unit-testbar): String-AKs → abhakbare Kriterien, verlustfrei; **Version-Bump ohne migrate würde das ganze Backlog verwerfen**. **`StoryDialog`** ist **self-contained** (liest `useBacklogStore`/`useBoardStore`/`useProjectStore` + `useConfirmDelete` selbst; Props nur `story`+`onOpenChange`) ⇒ triviales Andocken in 3 Seiten. Felder: Titel/Beschreibung/**Epic (Select, verschieben)**/Priorität/PT + **Akzeptanzkriterien als abhakbare Liste** (transaktional über Save-Patch wie Checkliste), **Aufgaben-Sektion via `StoryTasks`** (TASK-037), **Verlauf via `ActivityFeedPanel entityType="story"`**, **Löschen via `useConfirmDelete`** (Backlog- **und** Board-Slice-Snapshot/Undo, TASK-040/056). **Andockpunkte:** `BacklogTree` `StoryRow` bekommt `onClick`→`onOpenStory` (Inline-Zellen/**Griff**/Löschen stoppen `stopPropagation`, sonst würde Klick edit/löschen statt öffnen); **`SprintStoryCard` bekommt einen Grip-Handle** (`GripVertical`) – **nur der Griff** trägt `attributes/listeners`, Kartenkörper-`onClick` öffnet den Dialog; **Keyboard-D&D bleibt am Griff**; `SprintColumn` reicht `onStoryClick` durch; `SprintDetail` Story-Kopf ist jetzt ein Button. `BacklogView` rendert AKs als `criterion.text` (durchgestrichen bei `done`). Keine neue Dependency. Tests `tests/task-058/` (`acceptance` Transforms; `useBacklogStore` move/migration/logging; `StoryDialog` CRUD; `SprintStoryCard` Klick-vs-Drag) + `testkonzept.md`; Bestandsfixtures angepasst (007/008/037/052 AC-Objekte, 032/038 Artefakt-Stories bleiben `string[]`, 057 `onOpenStory`-Prop). E2E: `backlog.spec` (Dialog-Flow, Migration-Seed `version:1`) + `sprints.spec` (Drag jetzt am **Grip-Handle** `sprint-story-drag-*`). Gate grün (548 Tests, Build) + E2E (backlog 5, sprints 3, board 11, ceremonies 3) + CI. **⚠️ E2E-Falle:** Playwright `getByLabel(\"Titel\")` matcht **substring** (auch „Aufgabentitel\" aus der eingebetteten `StoryTasks`) ⇒ `{ exact: true }` nötig; jsdom-`getByLabelText` matcht dagegen exakt.
- 2026-07-03 · **TASK-057 – Backlog-Bereich `/backlog` unter Delivery** (PR #101 → `dev`, Task-Datei archiviert). Erster eigenständiger Backlog-Arbeitsbereich auf dem `useBacklogStore` (TASK-056), **erster Delivery-Nav-Eintrag** (Icon `ListTree`, Sidebar + ⌘K teilen `navigation.ts`) + per ⌘K erreichbar. Neue Route **`src/app/(dashboard)/backlog/page.tsx`** (hinter `useHydrated`-Gate): Projekt-Select (Muster `sprints`/`releases`), **Prioritäts- + Freitext-Filter** (`FilterBar` + `Input`), **`?project=<id>`-Deep-Link** via `initialProjectFromUrl` (liest `window.location.search` beim Mount – SSR-sicher hinter dem Gate, **kein** `useSearchParams`/Suspense; Basis TASK-061). Neu **`src/components/backlog/`**: **`BacklogTree`** (ein `DndContext`, je Epic ein `SortableContext`, Accordion **alle offen** default – Lehre TASK-052), **`EpicHeader`** (kompakter Collapse-Toggle als **eigener** `AccordionTrigger` ⇒ inline-Titel + Löschen sind **Geschwister**, keine verschachtelten Buttons), **`BacklogFooter`** (Octane-Aggregat). Story-Zeile: Griff (`GripVertical`, **nur der Griff** trägt die dnd-Listener ⇒ Zellen klickbar), Titel/PT via **`EditableTextCell`** (TASK-030), Priorität via **`EditableSelectCell`** (`StatusBadge`-Nodes), Rollup `doneTasks/total` (`storyTaskRollup`), Release-Platzhalter „–" (TASK-062). **Rank:** vertikales D&D **je Epic** (`reorderStory`); `handleDragEnd` ignoriert Drags über Epic-Grenzen (Map `storyId→epicId`). **CRUD:** „+ Epic"/„+ Story" als **kontrollierte `InlineAdd`** (aus `BacklogTree` exportiert; Enter fügt hinzu, Feld bleibt offen); Löschen via **`useConfirmDelete`** (TASK-040) mit **Backlog- UND Board-Slice-Restore** (`useBacklogStore.restore` + `useBoardStore.restore`) – Story-/Epic-Delete detacht Board-Tasks, Undo kehrt **beides** zurück. **Aggregat** neu `backlogSummary(stories, tasks, columns?)` in `src/lib/backlog.ts`: `plannedPt` = Σ Story-PT, `donePt` nur erledigte Stories (Done **ausschließlich** über `storyTaskRollup`/`isStoryDone`, TASK-038 – keine zweite Logik). **CommandBar:** neue Intent-Variante **`new-story`** + Aktion „Neue Story anlegen" → `/backlog`; Page öffnet das Add-Feld des ersten Epics **deklarativ** (`addStoryEpicId ?? (wantNewStory ? firstEpic : null)`, Schließen räumt Intent ab – Muster TASK-042, **kein setState-in-Effect**). Empty-States: kein Projekt → „Erste Idee anlegen"; Projekt ohne Items → „Story manuell anlegen" (legt Default-Epic an + öffnet Feld) + deaktiviertes „Planung mit Agent starten" (bis TASK-060). **Bewusst weggelassen:** **Tag-Filter** (Stories haben kein Tag-Feld – Tags leben auf Board-Aufgaben, TASK-031) und **Row-Click-Story-Dialog** (kommt mit TASK-058; Inline-Zellen decken Titel/Priorität/PT ab). Keine neue Dependency, keine Persist-Migration. Tests `tests/task-057/` (`backlog-summary` 3 + `BacklogTree` 7) + `e2e/backlog.spec.ts` (4, Store **direkt** geseedet ⇒ Beweis „ohne Pipeline": Seed+Footer+Add/Reload, Rank-Drag/Reload, Empty-State via Deep-Link, CommandBar) + `testkonzept.md`. Gate grün (541 Tests, Build) + E2E (backlog 4, board 11, sprints 3, ceremonies 3) + CI. **⚠️ CI-Falle (behoben):** `tsc --noEmit` typprüft **auch** Testdateien – ein `ReturnType<typeof vi.fn>`-getyptes Handlers-Objekt ist **nicht** auf die strikten Callback-Props zuweisbar; `vi.fn()`-Mocks stattdessen **direkt** als Props übergeben (Muster wie task-035). Lokal vor dem ersten Push nicht gefangen, weil die Testdatei beim ersten `tsc`-Lauf noch nicht existierte.
- 2026-07-02 · **TASK-056 – Backlog-Entities (Epic & Story als Store-Entitäten)** (PR #99 → `dev`, Task-Datei archiviert). **P0-Fundament** der Milestone-Gruppe „5++++ Backlog als Modul" (MUSS vor 057–062). Epics/Stories sind jetzt eigene, persistierte Entitäten im neuen **`useBacklogStore`** (`pm-studio-backlog` v1) statt Felder im Agenten-Artefakt – **ohne Pipeline** existent, CRUD-bar, ein Re-Run **ersetzt** nicht mehr. **Typen (`src/types/index.ts`):** Artefakt-Snapshot umbenannt zu **`BacklogEpic`/`BacklogStory`** (nested, `mock-templates` inhaltlich unverändert); neue **flache** `Epic { id, projectId, title, description?, rank }` + `UserStory` (+ `epicId`, `projectId`, `rank`, `description?`, `releaseId?`, `sourceRequirementIds?`, `provenance: agent|human|human_edited`); **Story-Ids 1:1 erhalten** ⇒ Board-/Sprint-`storyId` gültig, keine Migration dort. **Reine Helfer** neu `src/lib/backlog.ts`: `epicsForProject`/`storiesForProject`/`storiesForEpic` (rang-sortiert), `epicRollup`, `moveRanked` (Reorder), `flattenBacklog` (Artefakt→Store). **Store** (`src/store/useBacklogStore.ts`): add/update/remove Epic+Story (Kaskaden `removeEpic`→Stories weg + `detachStory`; `removeStory`→`detachStory`), `reorderEpic`/`reorderStory`, `removeProjectItems` (Projekt-Löschkaskade), `restore` (Undo), **`importBacklog`** (additiv & **idempotent** über Artefakt-Story-Id, überschreibt Edits nie, rankt neue Epics ans Ende), `migrateFromArtifacts` (einmalig, Flag `artifactsMigrated`). **`updateStory` hebt `agent`→`human_edited`** (Grundlage TASK-060; expliziter provenance-Patch gewinnt). **`useProjectStore`:** `removeIdea`-Kaskade an Backlog-Store, **`setArtifacts` detacht nicht mehr**; `runPipelineForIdea` ruft nach Scrum `importBacklog`. **Migration zyklusfrei:** `useBacklogStore` importiert **nicht** `useProjectStore` (das importiert den Backlog-Store für `removeIdea`) – die einmalige Übernahme liest die Alt-Artefakte **direkt aus `localStorage`** (`pm-studio-projects`) in `queueMicrotask`/`onRehydrateStorage` ⇒ hydrationsreihenfolge-unabhängig. **Alle Konsumenten** lesen aus dem Store (BacklogView[+`sprintSuggestions`-Prop], OverviewTab[+`projectId`], SprintDetail, sprints/releases/ceremonies/dashboard, `selectMetrics`/`selectVelocity`[+`stories`-Param], `search`[`SearchSources.artifacts`→`stories`]/CommandBar). `sprints/page` gated auf `stories.length === 0`. **Kein Activity-Log** für Backlog. Keine neue Dependency. Tests `tests/task-056/` (20) + Bestandstests angepasst (007/008/052 BacklogView, 053 SprintDetail, 037 detach, 009 pipeline, 011 selectors, 032 velocity, 042 search) + `testkonzept.md`. Gate grün (521 Tests, Build) + E2E (sprints 3, ceremonies 3, board 11) + CI.
- 2026-07-02 · **TASK-055 – Ceremonies-Bereich v2 (verlinkte Items + Kommentare)** (PR #96 → `dev`, Task-Datei archiviert). Reviews/Retros docken an konkrete Sprint-Items an und werden diskutierbar. **Modell additiv:** neuer Typ `CeremonyComment { id, author, text, createdAt }` + optionale `linkedStoryIds?`/`linkedTaskIds?`/`comments?` an **`CeremonyMeta`** (geteilt von Review **und** Retro) → **keine Persist-Version-Erhöhung** nötig (v2-Bestand bleibt gültig, Felder optional). **Reine Helfer** in `src/lib/ceremonies.ts`: `toggleLink` (Membership-Toggle), `resolveLinks(ids, items)` → `{id,label,missing}` (gelöschtes Item ⇒ `missing:true`, kein Absturz), `createComment` (Trim, `null` bei leerem Text, Autor-Fallback „Unbekannt", id/now injizierbar), `addComment`/`removeComment` (immutabel), `commentsChronological` (älteste zuerst, stabil). **Store** (`useSprintStore`): `toggleCeremonyStory`/`toggleCeremonyTask` + `addCeremonyComment`/`removeCeremonyComment`, je mit `kind:"review"|"retro"`; generischer **`patchEntry(state,kind,entryId,fn)`** mappt über die richtige Liste (Review/Retro teilen `CeremonyMeta`). **Kein Activity-Log** (Granularität gegen Rauschen, wie TASK-043/045). **UI:** neue **`CeremonyEnrichment`** (an **jedem** Historien-Eintrag): Sektion „Verknüpfte Items" (Link-Chips → Sprint-Detailseite `/sprints/<id>`, ✕ zum Entfernen; „Verknüpfen"-Picker mit Story-/Task-Buttons des Sprints, `aria-pressed`) + „Kommentare" (chronologische Liste, Autor+Datum, ✕; Add-Form mit optionalem Namen, Enter/Button, disabled bei leerem Text). **`CeremonyHistory`** bekommt **optionale** `sprintItems`+4 Callback-Props (⇒ TASK-054-Aufrufer/-Tests bleiben grün); `ceremonies/page.tsx` leitet linkbare Stories aus `artifacts[projectId].backlog` (gefiltert auf `sprint.storyIds`) und Tasks aus `useBoardStore` (`storyId ∈ storyIds`) ab. **TASK-054-Anlege-Dialog bleibt unverändert** (Verlinken/Kommentieren am Historien-Eintrag ⇒ minimales Regressionsrisiko). Verwaiste Links rendern als durchgestrichenes „Gelöschtes Item" ohne Anchor. Tests `tests/task-055/` (Helfer, Store-Actions Review/Retro getrennt, `CeremonyEnrichment` inkl. Missing-Item) + `e2e/ceremonies.spec.ts` (Verlinken+Kommentar+Reload) + `testkonzept.md`. Gate grün (501 Tests, Build) + Ceremonies-E2E (3) + CI.
- 2026-07-02 · **TASK-054 – Ceremonies-Bereich v1** (PR #94 → `dev`, Task-Datei archiviert). Review/Retro lösen sich vom „genau einer Dialog pro Sprint" (TASK-018) und werden ein **eigenständiger Bereich** mit historischer Liste. Neue Route **`src/app/(dashboard)/ceremonies/page.tsx`** (Nav-Gruppe „Delivery", Icon `ClipboardList`): chronologische Historie (`CeremonyHistory`) + „Neue Review/Retro"-Flow (`CeremonyDialog`) mit Sprint- (vorausgewählt) und **Scope-Auswahl** (`cross`/`team`/`project`; bei `team` ein `teamId` aus `usePeopleStore`). **Modellwechsel** im `useSprintStore`: von Upsert (`setReview`/`setRetro`) zu **Append-Liste** (`addReview`/`addRetro`, mehrere je Sprint). Neue Typen `CeremonyScope`/`CeremonyMeta`, `ReviewContent`/`RetroContent`; **`SprintReview = ReviewContent & CeremonyMeta`** (analog Retro). **Content vs. Meta getrennt:** `reviewFromForm`/`retroFromLists` liefern nur den Inhalt (Rückgabetyp `ReviewContent`/`RetroContent`, Body **unverändert** ⇒ TASK-018-Schema-Tests bleiben grün), `createReview`/`createRetro` (neu in **`src/lib/ceremonies.ts`**, id/now injizierbar) ergänzen die Metadaten; `teamId` nur bei Team-Scope (kein Dangling). Weitere reine Helfer dort: `ceremonyHistory` (Merge neueste-zuerst, stabiler Sort), `scopeLabel`, `isCeremonyInputValid`, `CEREMONY_SCOPES`. **Persist-Migration v1→v2** (`migrate`) backfillt TASK-018-Upsert-Einträge verlustfrei (id=`randomUUID`, `scope:"cross"`, `createdAt`=Epoch → in der UI „Datum unbekannt"). `ReviewForm`/`RetroForm` geben jetzt **Content** zurück (Aufrufer ergänzt Meta); **Klemmbrett-Flow auf `/sprints` bleibt** und legt Einträge mit `scope:"project"` an. Status-Farbe nur über `<StatusBadge>` (Review=info, Retro=success), Scope als neutraler Chip. Seite hinter `useHydrated`-Gate (Datum/Persist SSR-sicher). Keine neue Dependency. Tests `tests/task-054/` (ceremonies-Helfer, Store-Migration, `CeremonyHistory`) + `e2e/ceremonies.spec.ts` (Review anlegen→Historie→Reload, Retro anlegen) + `testkonzept.md`; TASK-018-Store-Test auf Append-Modell angepasst. Gate grün (478 Tests, Build) + E2E (ceremonies 2, sprints 3) + CI.
- 2026-07-02 · **TASK-053 – Sprint-Detailseite** (PR #92 → `dev`, Task-Datei archiviert). Ein Sprint als **eigene Seite** statt nur einer Spalte. Neue dynamische Route **`src/app/(dashboard)/sprints/[id]/page.tsx`** (Server-Component reicht `id` an Client durch, Muster wie `projects/[id]`) + neu **`src/components/sprint/SprintDetail.tsx`**: Back-Link, Kopf (Name, Ziel, Zeitraum via `formatSprintRange`, Status- + abgeleitetes „Aktiv"-Badge nach `useHydrated`-Gate), Fortschritts-Karte (`sprintProgress` + `ProgressBar`), Burndown (`SprintBurndownChart`/`sprintBurndown`) und **klickbare Story-/Task-Liste** (`tasksForStory`) – jeder Task-Button öffnet denselben **`TaskDialog`** wie das Board (`onSave → updateTask`, `onDelete → useConfirmDelete`/TASK-040). **Done/Fortschritt ausschließlich** über TASK-038-Quelle (`isStoryDone`) – keine Doppellogik. Navigation: neues optionales **`detailHref`**-Prop an `SprintColumn` rendert ein Öffnen-Icon (↗, testid `sprint-detail-link-<id>`) **nur bei realen Sprints**; `sprints/page.tsx` reicht `/sprints/<id>` durch. **Scrollbar-Fix:** neue **`.thin-scrollbar`**-Utility in `globals.css` (themed via `scrollbar-color` + `::-webkit-scrollbar`, Border-Token) auf die horizontale Spaltenreihe der Übersicht ⇒ keine weiße System-Scrollbar. Not-Found-/Empty-States, Status-Farben nur über `<StatusBadge>`. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-053/SprintDetail.test.tsx` (5) + `e2e/sprints.spec.ts` (Detail öffnen/Task-Klick/Back) + `testkonzept.md`. Gate grün (461 Tests, Build) + Sprint-E2E (3) + CI. **⚠️ Nebenbefund:** der Projekt-Seed in `e2e/sprints.spec.ts` war auf Version 1 **ohne `risks`** und crashte an der v1→v2-Risk-Migration (TASK-045) → auf Version 2 + `risks:{risks:[]}` angehoben. **`e2e/my-work.spec.ts` hat denselben Seed-Bug (Version 1, kein `risks`) – noch offen, out of scope; CI führt kein E2E aus, daher unkritisch für Merges, aber `npx playwright test e2e/my-work.spec.ts` schlägt lokal fehl, bis der Seed auf Version 2 + `risks` gehoben wird.**
- 2026-06-22 · **TASK-052 – Story-Aufgaben auffindbar machen (Discoverability)** (PR #89 → `dev`, Task-Datei archiviert). Story-Aufgaben (TASK-037) waren vier Ebenen tief vergraben + ohne Pipeline unsichtbar. **`BacklogView.tsx`:** Akkordeon öffnet jetzt **alle Epics** by default (`backlog.epics.map(...)` statt `slice(0,1)`); pro Story klickbare **Aufgaben-Count-Badge** (`data-testid="story-task-count-<storyId>"`, „N Aufgaben"/„1 Aufgabe"/„Keine Aufgaben", Count = `tasks.filter(storyId===…)`), die per `scrollIntoView` (**Optional-Call** `?.scrollIntoView?.({...})` ⇒ jsdom-sicher) zur Sektion **`#story-tasks-<storyId>`** springt. Neuer Export **`BacklogEmpty`** (dummes Presentational-Component: Empty-State „Noch kein Backlog" + CTA „Pipeline ausführen", Props `onRunPipeline`/`isRunning`). `inBoard`-Semantik unverändert (Story hat ≥1 Task). **`StoryTasks.tsx`:** Wrapper bekommt Anker-`id="story-tasks-<storyId>"` + `scroll-mt-20`, Überschrift „Aufgaben" mit `ListChecks`-Icon. **`ProjectDetail.tsx`:** Backlog-Tab nutzt ohne Artefakte `BacklogEmpty` (CTA → `runPipelineForIdea(idea)`) statt generischem `TabEmpty`. **Kein Datenmodell-Change**, keine neue Dependency, keine Persist-Migration. Count-Badge zeigt bewusst nur die **Gesamtzahl** – detailliertes Roll-up (`doneTasks/total · PT`) bleibt einzig in `StoryTasks` (keine zweite Terminal-/PT-Logik in `BacklogView`). Tests `tests/task-052/BacklogView.test.tsx` (alle Epics offen, Count Plural/Singular/leer, Scroll-Klick gestubbt, `BacklogEmpty` CTA + Disabled) + `testkonzept.md`. Gate grün (455 Tests, Build) + CI. Kein E2E deckt den Backlog-Tab ab (Pipeline-Setup).
- 2026-06-22 · **TASK-051 – „+ Spalte"-Kachel am Board** (PR #87 → `dev`, Task-Datei archiviert). Rechts neben der letzten Kanban-Spalte eine gestrichelte **„+ Spalte"-Kachel** zum schnellen Anlegen einer Phase – zusätzlich zum `ColumnManager`. Neu **`src/components/board/AddColumnTile.tsx`** (dumb, Client-State **analog `QuickAddTask`**): Trigger „+ Spalte" → Inline-`<Input>`, **Enter** ruft `onAdd(label)` getrimmt und hält das Feld **offen/leer**, **Esc/Blur** schließt, leere Labels ignoriert (testids `add-column-trigger`/`add-column-input`). `board/page.tsx` rendert sie als **letztes Element** der Spaltenreihe – **nur flache Kanban-Ansicht** (`groupBy === "none"`, nicht Liste/Swimlanes, wie Quick-Add) – und ruft **`useBoardColumnsStore.addColumn({ label })`** (**keine neue Action**): neue Phase landet **ans Ende, nicht-terminal**, Default-Status `idle` (Store-Default); **Done-Semantik (`terminalColumnIds`) unberührt**. Keine neue Dependency, **keine Persist-Migration** (Store/`pm-studio-board-columns` kann das bereits). Tests `tests/task-051/` (`AddColumnTile` Trigger/Enter/Trim/leer/offen-bleiben/Esc/Blur; `add-column-default` Kachel-Kontrakt `addColumn({ label })` ⇒ nicht-terminal ans Ende – breiteres `addColumn` bleibt in task-032) + `e2e/board.spec.ts` (Anlegen erscheint sofort + überlebt Reload) + `testkonzept.md`. Gate grün (449 Tests, Build) + Board-E2E (11) + CI.
- 2026-06-22 · **TASK-045 – Editable Risk Register** (PR #85 → `dev`, Task-Datei archiviert). Risikoregister ist nicht mehr read-only Agenten-Output, sondern **manuell pflegbar** (Owner/Status/Maßnahme/Eskalation, anlegen/bearbeiten/löschen). Neuer Typ **`RiskStatus = open|mitigating|monitoring|closed`**; `RiskEntry` **additiv** um `owner?`/`status` erweitert, `mitigation?`/`escalation?` jetzt **optional**. Reine Helfer neu in **`src/lib/risk.ts`**: Status-Meta `RISK_STATUS`/`RISK_STATUS_ORDER` + `riskStatusBadge` (Farbe **single-sourced** über `<StatusBadge>`, spiegelt SPRINT_STATUS/RELEASE_STATUS) und Transforms `createRisk` (**injizierbare id** ⇒ deterministisch), `addRisk` (Blank-Titel ignoriert), `updateRisk` (id erhalten), `removeRisk`, `riskFormValues`, `withRiskStatus` (Migrations-Backfill). **Transaktional** über das Save-Patch des Projekt-Artefakts: neue Action **`useProjectStore.setRisks(ideaId, risks)`** (no-op ohne Artefakte) – `RiskTable` reicht das **gesamte** nächste Array via `onChange` durch; Abbrechen verwirft (lokaler Dialog-State). **Persist-Migration v1→v2**: backfillt fehlenden `status` auf „open" (`withRiskStatus`), damit vor dem Feature persistierte Agenten-Risiken gültig/editierbar bleiben; `mock-templates` setzt `status: "open"` direkt. UI: neuer **`RiskDialog`** (Add/Edit, alle Felder), interaktive **`RiskTable`** (Spalten Owner/Status/Eskalation, „Risiko hinzufügen", Bearbeiten je Zeile, Löschen via **`useConfirmDelete`**/TASK-040 mit Array-Snapshot-Undo, Empty-State); `ProjectDetail` verdrahtet `onChange → setRisks`. **Risiko-Änderungen werden bewusst NICHT geloggt** (Granularität gegen Rauschen, TASK-043; spätere Anbindung via `logActivity` möglich). Keine neue Dependency. Tests `tests/task-045/` (risk-Transforms+Status-Meta, `setRisks`+Migration, `RiskTable`-Render/Add/Edit) + `testkonzept.md`; Bestandsfixtures task-007/task-011 um `status` ergänzt. Gate grün (442 Tests, Build) + CI.
- 2026-06-22 · **TASK-043 – Activity Feed & Decision Log** (PR → `dev`, Task-Datei archiviert). Nachvollziehbare Historie je Entität + Log menschlicher Entscheidungen. Neue Typen `ActivityEvent { id, entityType, entityId, kind (create|update|delete|review|run), summary, actor (human|agent), createdAt }` + `Decision { id, context, choice, rationale?, actor, createdAt, relatedEntity? }`. **Zentrales, append-only Schreiben** über **einen** Pfad: neu **`useActivityStore.log(input)`** + Wrapper **`logActivity(input)`** – der Store ist ein **Leaf** (importiert keine andere Store-Datei), daher rufen die Mutations-Actions in `useProjectStore`/`useBoardStore`/`useSprintStore`/`useReleaseStore` ihn **außerhalb** des `set`-Updaters auf (kein Modul-Zyklus, Updater bleiben rein; `get()` liefert Namen/Titel **vor** dem Löschen). **Granularität bewusst gegen Rauschen:** create/delete für alle vier Entitäten; *update* nur bedeutsam – Task-**Spaltenwechsel** (`moveTask` **und** `updateTask`, mit Phasen-Label via `columnLabel`), Sprint-Update, Release-Update. **Reorder in derselben Spalte, reine Feldedits und Cascade-Detaches erzeugen kein Event.** Reine Helfer neu in **`src/lib/activity.ts`** (`createActivityEvent` mit injizierbarer id/now ⇒ deterministisch, `eventsForEntity` neueste-zuerst, `ACTIVITY_KIND_META` → `<StatusBadge>`) und **`src/lib/decision.ts`** (`createDecision` trimmt + `null` bei leerem Kontext/Wahl, `decisionsForEntity`). Neuer **`useDecisionStore`**. Panels neu in `src/components/activity/`: **`ActivityFeedPanel`** (read-only, `limit`-Prop) + **`DecisionLogPanel`** (Inline-Add) – beide hinter `useHydrated`-Gate; Store-Array **stabil selektiert** + via `useMemo` abgeleitet (⚠️ direkt `eventsForEntity(...)` aus dem Selektor zurückgeben ⇒ zustand-„getSnapshot"-Loop, vgl. TASK-031). Eingebettet: **ProjectDetail**-Tab „Verlauf" (Feed + Decisions), **`TaskDialog`** (Feed, `limit=8`), **`SprintDialog`**/**`ReleaseDialog`** je nur im **Edit-Modus** (Feed + Decisions). Persist additiv (`pm-studio-activity`/`pm-studio-decisions`, v1), keine neue Dependency, keine Migration. Tests `tests/task-043/` (25: activity/decision reine Funktionen, Store-Instrumentierung inkl. Granularität, Decision-Store, `ActivityFeedPanel`-Render) + `testkonzept.md`. Gate grün (422 Tests, Build).
- 2026-06-17 · **TASK-042 – My Work & globale Suche** (PR #82 → `dev`, Task-Datei archiviert). Zwei Lücken geschlossen. **(1) My Work** – neue Route `/my-work` (Nav „Meine Aufgaben" in der Overview-Gruppe) mit projekt-/storyübergreifender Sicht auf die Arbeit **einer Person**. Reine Selektoren in neu **`src/lib/my-work.ts`**: `tasksForPerson`, `isTaskOpen`, `isTaskOverdue` (**delegiert an `src/lib/due.ts`** – keine zweite Überfällig-Logik), `myWorkCounts`, `filterMyWork` (Scope `assigned|open|overdue`), **`resolveMyWorkPersonId(preferredId, persons)`** (kapselt die Person-Auflösung → späterer Login setzt nur `preferredId`, Seite bleibt unverändert; Fallback erste Person). Done über `terminalColumnIds` (TASK-032). Die Seite wiederverwendet `BoardListView`/`buildBoardRows`/`FilterBar` (Person- + Scope-Select) + drei Zähl-Chips (zugleich Scope-Schnellfilter, `data-testid="my-work-chip-<label>"`); Empty-States via `useHydrated`-Gate + „keine Personen"-Karte mit Link zum Team; Safe-Delete aus dem `TaskDialog` über `useConfirmDelete` (TASK-040). **(2) Globale Suche** – indexfreie **`searchEntities(query, sources)`** in neu **`src/lib/search.ts`** über Projects/Tasks/Stories/People/Releases (case-insensitiv, pro Art gekappt `limitPerKind=5`, Navigations-`href`; Story-Sublabel = Projektname, Person auch über Rolle, kind-namespaced Keys). `CommandBar` umgebaut: **`Command shouldFilter={false}`** + kontrollierte Query, Treffer nach Art gruppiert, Routen/Aktionen manuell gefiltert; Such-Button mit `data-testid="command-trigger"`. **Create-Aktionen:** „Neue Idee" → `/ideas/new`; „Neue Person"/„Neues Release" navigieren zur Zielseite und öffnen deren Dialog **deklarativ** über neuen ephemeren **`useCommandActionStore`** (Intent wird im Render von `team`/`releases` ausgelesen, beim Schließen geleert) – bewusst **kein `setState` im Effect** (sonst `react-hooks/set-state-in-effect`), kein URL-Param. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-042/` (my-work + search reine Funktionen, 22) + `e2e/my-work.spec.ts` (5: My-Work-Filter/Personenwechsel, Such-Navigation, zwei Create-Aktionen) + `testkonzept.md`. Gate grün (397 Tests, Build) + E2E.
- 2026-06-17 · **TASK-041 – Release-Fortschritt & Timeline** (PR #79 → `dev`, Task-Datei archiviert). Releases sind jetzt ein Management-Objekt mit **Status**, **Scope**, **Fortschritt** und **Sprint-Timeline** statt nur Sprint-Anzahl. Neues Feld **`Release.status`** (`ReleaseStatus = planned|active|done`), Persist-Migration **v1→v2** additiv (`migrate`: Altbestand ohne Status ⇒ `planned`, `LegacyRelease`-Typ in `useReleaseStore`); Farbe **ausschließlich** über `<StatusBadge>` via neuer Map **`RELEASE_STATUS`/`RELEASE_STATUS_ORDER`** (`src/lib/release-meta.ts`, spiegelt `SPRINT_STATUS`). **Fortschritt = reine Funktion `releaseProgress(releaseId, sprints, stories, boardTasks, terminalColumns)`** (`src/lib/release.ts`): summiert `sprintProgress` über die Release-Sprints → `{donePt, plannedPt, doneCount, total, sprintCount}`; baut **nachweislich** auf TASK-038 (`isStoryDone` via `sprintProgress`) auf – **keine zweite Done-Logik**, kein Doppelzählen (Story lebt in ≤1 Sprint). Timeline-Helfer **`releaseSprints`** (gefiltert + nach `order` sortiert). `ReleaseCard` zeigt Status-Badge, Scope (`n Stories · PT`), Progress-Text + `ProgressBar` (donePt/plannedPt) und die Sprint-Timeline (aktiver Sprint via `isActiveSprint` hervorgehoben, `today` SSR-sicher **nach** dem `useHydrated`-Gate, Empty-State „Noch keine Sprints"); `ReleaseDialog` mit Status-Select. `releases/page.tsx` leitet Stories aus `artifacts[activeProjectId]` + terminale Spalten (`terminalColumnIds`) ab und reicht `progress`/`sprints`/`today` durch. Keine neue Dependency. Tests `tests/task-041/` (release-progress reine Funktion inkl. TASK-038-Fälle, `ReleaseCard`-Render, Store-Migration) + `testkonzept.md`; Bestandsfixtures `task-025`/`task-040` um `status` ergänzt. Gate grün (375 Tests, Build) + Playwright (13).
- 2026-06-17 · **TASK-040 – Sichere Lifecycle-Aktionen (Confirm/Undo/Toast)** (PR → `dev`). Eine **gemeinsame** Mechanik statt Ad-hoc-Dialoge: `useConfirmStore` (imperatives Promise `confirm(options)`) + `ConfirmDialog`-Host, `useToastStore` (ephemer, kein Persist) + `Toaster` (Undo-Action, Auto-Dismiss 8 s im Component-Timer), gebündelt im Hook **`useConfirmDelete`** (`src/components/common/`) als Ablauf **bestätigen → löschen → Toast mit Undo**; beide Hosts hängen **einmalig** im `(dashboard)/layout`. **Undo = Snapshot + `restore`:** jeder betroffene Store hat eine `restore(slice)`-Action (`useBoardStore.restore(tasks)`, `usePeopleStore.restore({persons,teams})`, `useProjectStore.restore({ideas,artifacts})`, `useReleaseStore.restore(releases)`, `useSprintStore.restore(sprints)`, `useBoardColumnsStore.restore(columns)`), die die vorherige Slice **1:1** zurückspielt (Cascade inklusive). **Referenz-Aufräumung beim Löschen:** `usePeopleStore.removePerson` ruft jetzt `useBoardStore.detachAssignee(id)` (reiner Helfer `detachAssignee` in neu `src/lib/assignment.ts`) → **kein dangling `assigneeId`** mehr; exakt das Muster `useTagStore.removeTag`→`useBoardStore.detachTag`. Konsumenten verdrahtet: **Projekt/Idee** (neuer „Löschen"-Header-Button in `ProjectDetail`, vorher gab es **keinen** UI-Lifecycle → nach Delete `router.push('/projects')`), **Board-Task** (neues Pflicht-Prop `onDelete` im `TaskDialog`, Handler `handleDeleteTask` in `board/page.tsx`), **Person/Team** (`team/page.tsx`), **Release** (`releases/page.tsx`), **Board-Phase** (`ColumnManager`). Keine neue Dependency, **keine Persist-Migration** (Toast/Confirm ephemer, `restore` ersetzt nur Slices). Tests `tests/task-040/` (assignment-Helfer, Toast-/Confirm-Store, Cross-Store-Cascade+Undo, voller UI-Fluss `confirm-delete-flow`) + Board-E2E „deletes a task via confirm … undo" + `testkonzept.md`; Bestandstests **angepasst statt gelöscht**: `tests/task-032/ColumnManager` (Delete jetzt hinter Confirm), `tests/task-035/TaskDialog` (neues `onDelete`-Prop). Gate grün (361 Tests, Build) + Board-E2E (10).
- 2026-06-17 · **TASK-038 – Story-Done-Semantik korrigiert** (PR #76 → `dev`). `isStoryDone` (`src/lib/sprint-progress.ts`) zählt eine Story nur noch als fertig, wenn sie **≥1 Task** hat und **alle** Story-Tasks terminal sind (vorher `.some(... terminal)`); **0 Tasks ⇒ offen** (kein leeres-`every()`-`true`). Bleibt die **eine** Done-Quelle: `storyTaskRollup.storyDone` (`story-tasks.ts`), `sprintProgress`, `selectVelocity` (`dashboard-selectors.ts`) und `sprintBurndown` konsumieren sie weiter – keine zweite `.some/.every`-Logik. In `burndown.ts` wurde die dortige zweite Done-Entscheidung in `storyDoneDate` durch `isStoryDone` ersetzt; **Done-Datum = spätestes `doneAt`** (Story erst mit letztem Task fertig) statt vorher frühestem. UI-Badges (`StoryTasks`/`SprintColumn`/`SprintStoryCard`) erben die Regel. Bestandstests angepasst statt gelöscht: `tests/task-037` (1/3 ⇒ `storyDone:false`), `tests/task-032` (1/2, „done"≠terminal ⇒ `false`), `tests/task-026` (Burndown `[3,0]`); `tests/task-021`/`tests/task-011` bleiben grün (je 1 Task/Story). Neu `tests/task-038/` (0/1-von-2/2-von-2 über alle Konsumenten) + `testkonzept.md`. Gate grün (343 Tests, Build) + Board-/Sprint-E2E. Keine neue Dependency, keine Persist-Migration.
- 2026-06-17 · **Backlog vollständig in `/tasks` überführt** (2 Planungs-PRs, nur Doku/Task-Dateien, kein Code):
  - **PR #72** – Welle „PM-Korrektheit": `tasks/TASK-038…042` (038 Story-Done P0, 040 Lifecycle, 041 Release-Progress, 042 My Work+Search). **TASK-039** = bereits durch ehem. TASK-018 erledigt (keine Datei).
  - **PR #74** – 2. Welle (Rest der 3 Notizen): `043` Activity/Decision-Log, `045` Risk-Register, `051` „+Spalte" (Quick Win), `052` Story-Discoverability (Quick Win), `053` Sprint-Detailseite, `054`/`055` Ceremonies v1/v2.
  - **Reserviert, nicht neu vergeben:** `044` (Dependencies), `046–050` (= bestehende TASK-012/013/014/015/016, Agenten/Workflow). **Noch unnummeriert & offen:** ui-backlog **A** (Octane-Listenansicht) und **D** (Story-Edit aus Sprint/Backlog).
  - **Keine feste Kette** beschlossen – pro Task einzeln umsetzen. Empfohlener Einstieg unverändert: **TASK-038**.
- 2026-06-16 · **TASK-018 – Retro- & Review-Formulare** abgeschlossen (PR #69 → `dev`, Task-Datei archiviert). Sprint am Ende reflektieren: **Review** (Freitext „geliefert", erreichte vs. **abgeleitete** geplante PT, Notizen) + **Retro** (drei Listen „Lief gut/Verbessern/Aktionen"). Neue Typen `SprintReview`/`SprintRetro` (`src/types/index.ts`; geplante PT werden **nicht** gespeichert, sondern aus der Sprint-Zuordnung abgeleitet). `useSprintStore`: `reviews`/`retros` persistiert, `setReview`/`setRetro` als **Upsert** über `sprintId` (genau ein Eintrag/Sprint), `removeSprint` räumt beide mit auf (Partialize erweitert, Persist-Version **bewusst bei v1** – additiv, shallow-merge reicht; Bump ohne `migrate` würde Bestand verwerfen). Reine/​testbare Schema-+Mapping-Helfer in neu `src/lib/sprint-review-schema.ts` (zod **v4**: `{ error: … }` statt `invalid_type_error`; `reviewFormSchema`, `reviewFromForm`/`reviewFormDefaults`, `retroFromLists`/`cleanRetroItems`/`retroListsFromRetro`, `RETRO_COLUMNS`). UI: `ReviewForm` (react-hook-form + zod, `achievedPt` via `valueAsNumber` ⇒ leer = NaN ⇒ Fehler), `RetroForm` (lokale Listen, Add per +/Enter, Remove ✕), zusammengeführt in `SprintReviewDialog` (Tabs Review/Retro, gekeyt per Sprint-Id). Einstieg: **Klemmbrett-Icon** in `SprintColumn`, **nur** für Status `active`/`done`; `sprints/page.tsx` hält Dialog-State + leitet `plannedPt` aus den zugeordneten Stories ab. Keine neue Dependency. Tests `tests/task-018/` (Store-Upsert + Cleanup; Schema-Validierung + Mapping) = 17 Tests + `testkonzept.md`.
- 2026-06-16 · **TASK-036 – Swimlanes (Gruppierung im Board)** abgeschlossen (PR #67 → `dev`). Kanban-Board optional in horizontale **Swimlanes** nach **Assignee/Sprint/Tag**; „Keine" = heutige flache Ansicht. Reine Gruppierung in neu `src/lib/swimlanes.ts`: `SwimlaneMode = none|assignee|sprint|tag`, `buildSwimlanes(tasks, mode, refs)` → `Swimlane[]` (Lanes in Store-Reihenfolge der Referenz-Entität, „ohne Zuordnung"-Lane `UNGROUPED_LANE` immer **zuletzt**, **keine leeren Lanes**; Sprint via `storyId`→Sprint-Map, **Tag via erstem Tag `tagIds[0]`** ⇒ jeder Task in genau einer Lane = eindeutige dnd-Ids); `laneReassignment(mode, laneId)` → `{kind:"assignee",assigneeId}` / `{kind:"sprint",sprintId}` / `{kind:"none"}`. Auswahl `groupBy` persistiert im `useBoardStore` (Partition + Persist **v7→v8 additiv**, Default `none`, `setGroupBy`). Neu `BoardSwimlanes` (dumm; wiederverwendet `KanbanColumn` je Lane, Lane-Header mit Count/PT, Empty-State „Keine Tasks zum Gruppieren") + `SwimlaneSelect`-Umschalter (Radiogroup „Gruppieren nach", **nur Kanban-Ansicht**). **D&D lane-aware über Drag-`data` statt Id-Parsing:** `KanbanColumn`-Droppable trägt `{type:"column",columnId,laneId}` (Droppable-Id + testids per Lane namespaced: `<laneId>__<colId>` bzw. testid `board-column-<laneId>-<colId>`; flache Ansicht ohne `laneId` = unveränderte bare Ids), `TaskCard`-Sortable trägt `{type:"task",taskId,columnId,laneId}`. `board/page.tsx#handleDragEnd` liest `over.data.current` → `moveTask(activeId, columnId, beforeId)`; bei `laneId !== undefined` zusätzlich `laneReassignment`: Assignee → `updateTask({assigneeId})`, Sprint → `assignStory(storyId, …)` **nur mit storyId**, **Tag → nur Spalte** (Mehrfach-Tags im `TaskDialog` gepflegt). Keine neue Dependency. Tests `tests/task-036/` (swimlanes-Helfer, board-store-groupby, `SwimlaneSelect`, `BoardSwimlanes` mit `DndContext`-Wrapper) + E2E „groups the board into swimlanes … survives a reload".
- 2026-06-16 · **TASK-032 – Anpassbare Kanban-Phasen** abgeschlossen (032a PR #64, 032b PR #65 → `dev`, Task-Datei archiviert). **032a:** Board-Phasen sind Daten (`BoardColumnDef { id, label, status, order, wipLimit?, isTerminal }`, `BoardColumn` = freie String-Id); neuer `useBoardColumnsStore` (persist `pm-studio-board-columns` v1 = `DEFAULT_BOARD_COLUMNS`) mit CRUD (add/rename/setStatus/setWip/setTerminal[schützt letzte terminale]/reorder/remove[verschiebt Tasks via neuer `useBoardStore.reassignColumn` auf Fallback, schützt letzte/letzte-terminale]). `src/lib/board.ts` nur noch `DEFAULT_BOARD_COLUMNS` + spaltenbewusste Helfer (`findColumn`/`columnLabel`/`columnStatus`/`isWipExceeded(def,count)`) – die 3 alten Konstanten entfallen. Neu `ColumnManager` („Spalten verwalten": Inline-Rename, Status-Select, WIP-Feld, „Abgeschlossen"-Toggle, ▲/▼-Reorder, Löschen). Alle Konsumenten lesen Phasen aus Store (`board/page`, `KanbanColumn` [nimmt `column: BoardColumnDef`], `BoardListView` [Status-Optionen via `meta`], `TaskDialog`, `StoryTasks`) bzw. als Parameter (reine `board-rows`/`story-tasks`, Default = Standardphasen). **032b:** Done-Semantik via `isTerminal` statt literal `column === "done"`: neue Helfer `terminalColumnIds(columns)`/`DEFAULT_TERMINAL_COLUMN_IDS`/`isTerminalColumn`. Ersetzt in `stampDone` (`doneAt`), `sprintBurndown`, `isStoryDone`/`sprintProgress`, `storyTaskRollup`, `isOverdue`/`filterBoardTasks`/`storyMatchesFilter`, `dashboard-selectors` (Donut `selectTaskStatusDistribution` jetzt nach Status-Akzent gebucketet → **kein Crash** bei Custom-Spalten-Id; `selectProjects`/`selectVelocity` terminal-aware). Terminale Ids durchgereicht: `TaskCard.isTerminal`, `BoardListView`-Due-Zelle, `SprintColumn.terminalColumns`, Dashboard/Sprints/Board-Seite via `terminalColumnIds(boardColumns)`. Alle Helfer defaulten auf `done`. Keine neue Dependency. Tests `tests/task-032/` (Store-CRUD, dynamic-columns, ColumnManager, is-terminal, board-store-terminal) + E2E (Phase anlegen überlebt Reload); `tests/task-027`+`tests/task-030` angepasst.
- 2026-06-16 · **TASK-035 – Checklisten/Subtasks auf Task-Ebene** abgeschlossen (PR #61 → `dev`, Task-Datei archiviert PR #62): Checkliste **innerhalb** eines Board-Tasks (abzugrenzen von Story→Aufgaben TASK-037). `ChecklistItem { id, text, done }` + `BoardTask.checklist?` (additive Persist-Migration **v6→v7**). Reine Logik in neu `src/lib/checklist.ts`: `checklistProgress(items)` → `{done,total}` (fehlend = 0/0), `checklistComplete`; Array-Transforms `addChecklistItem` (Trim, leer ignoriert, `crypto.randomUUID`), `toggleChecklistItem`, `renameChecklistItem` (**ohne** Trim – erlaubt Zwischenstände), `removeChecklistItem`, `normalizeChecklist` (trimmt, leere raus, `undefined` wenn nichts bleibt). Neu `src/components/board/ChecklistProgress.tsx` (`ChecklistProgressBadge`, kompakte „n/m"-Pille, grün bei vollständig via `STATUS_STYLES.success`, rendert nichts ohne Checkliste) auf `TaskCard` + Spalte „Checkliste" in `BoardListView` (`BoardRow.checklist`). Editor im `TaskDialog`: Checkbox (`role="checkbox"`)/Inline-`Input`/✕ je Punkt, neues Feld + Enter, Mini-`ProgressBar` (TASK-028). **Persistenz transaktional** über bestehendes Save-Patch (`checklist: normalizeChecklist(...)`) → **keine** neuen Store-Actions (bewusst abweichend von der „Betroffene Dateien"-Skizze: konsistent mit Tags/Fälligkeit). Keine neue Dependency. Tests `tests/task-035/` (Helfer + `ChecklistProgressBadge` + `TaskDialog`-CRUD).
- 2026-06-16 · **TASK-034 – Fälligkeitsdaten & Überfällig-Hinweis** abgeschlossen (PR #59 → `dev`): `BoardTask.dueDate?` (ISO `YYYY-MM-DD`, additive Persist-Migration v5→v6). Reine Logik in neu `src/lib/due.ts`: `isOverdue(dueDate, today, done)` (Termin < heute & offen; lexikografischer ISO-Vergleich, kein Date/Zeitzonen-Handling), `dueStatus` → `overdue|today|upcoming` (erledigte Tasks **immer** neutral), `DUE_STATUS_TOKEN` (Zustand → StatusBadge-Token danger/warning/neutral), `formatDueDate` (`DD.MM.`). Neu `src/components/board/DueBadge.tsx` (kompakte Pille, Kalender-Icon; Farbe **nur** über `STATUS_STYLES` = gleiche Quelle wie `<StatusBadge>`, `upcoming` neutral `bg-secondary`) – auf `TaskCard` und als sortierbare Spalte „Fällig" in `BoardListView` (`BoardRow.dueDate`). `TaskDialog`: Feld „Fällig am" (`type="date"`, leer ⇒ `undefined`). Filter: Sentinel `OVERDUE` in `board-filters.ts` (`BoardFilter.due`, optionaler `today`-Param von `filterBoardTasks`) + Control „Fälligkeit" in `board/page.tsx`. **SSR-sicher:** „heute" (`toLocaleDateString("sv-SE")`) erst nach dem Hydration-Gate berechnet und per Prop an `KanbanColumn`/`TaskCard`/`BoardListView` durchgereicht (Muster wie `isActiveSprint`). Keine neue Dependency. Tests `tests/task-034/` + E2E `e2e/board.spec.ts` (Überfällig-Badge + Filter). **Task-Datei archiviert** (auch TASK-033 nachträglich archiviert, PR #58).
- 2026-06-16 · **TASK-033 – Quick-Add direkt in der Spalte** abgeschlossen (PR #56 → `dev`): Am Fuß jeder Kanban-Spalte schnelles Anlegen ohne `TaskDialog`. Neu `src/components/board/QuickAddTask.tsx` (dumb wie `EditableTextCell`): „+ Task hinzufügen"-Trigger → Inline-`<Input>`; **Enter** ruft `onAdd(trimmedTitle)` und hält das Feld **offen/leer** für den nächsten Eintrag, **Esc/Blur** schließt, leere/whitespace-Titel werden ignoriert; Prop `disabled` rendert `disabledHint` statt des Triggers (testids `quick-add-trigger|input|disabled-<col>`). `KanbanColumn` rendert es am Spaltenfuß und reicht `onQuickAdd(column,title)` / `quickAddEnabled` / `quickAddDisabledHint` durch (nur sichtbar, wenn `onQuickAdd` gesetzt). `board/page.tsx`: **aktives Projekt aus dem Filter** (`filter.projectId === ALL ? undefined : ideas.find(...)`) → bei `ALL` deaktiviert mit Hinweis „Projekt wählen, um Tasks anzulegen" (Board-Task braucht zwingend `projectId`/`projectName`); `handleQuickAdd(column,title)` baut den Task via `useBoardStore.addTask` (richtige Spalte/`order` automatisch, Default-Priorität `"mittel"`, Projektbezug). Keine neue Dependency, keine Persist-Migration. `addTask`-Order-Semantik bereits in `tests/task-008` getestet → nicht dupliziert. Tests `tests/task-033/QuickAddTask.test.tsx` (6) + E2E `e2e/board.spec.ts` (Disabled-Zustand, Anlegen nach Projektwahl, Counter, Persistenz nach Reload).
- 2026-06-15 · **TASK-030 – Inline-Edit Liste** abgeschlossen (PR → `dev`): Board-Listenansicht (`BoardListView`) inline editierbar. Neu `src/components/board/cells/`: **`EditableTextCell`** (Titel/PT; Wert als Button → Inline-`<Input>`, Enter/Blur committen, Esc bricht ab; Caller trimmt/validiert den Rohstring – PT ≥ 0, leer ⇒ `estimate_pt: undefined`, negativ verworfen) und **`EditableSelectCell`** (Status/Priorität/Assignee/Sprint als randloser `<Select>`-Trigger + Dropdown; `disabled` ⇒ statischer Platzhalter; ⚠️ base-ui `onValueChange` liefert `string | null` → null abfangen). **Beide stoppen `onClick`-Propagation** ⇒ Zellklick editiert, übrige Zeile öffnet weiter den `TaskDialog` (`onRowClick` bleibt auf der `<tr>`). Handler/Lookups (`onUpdateTask`, `onAssignSprint`, `persons`, `sprints`) laufen über die TanStack-**`meta`** an die Zell-Renderer (`table.options.meta as BoardListMeta`). Status/Priorität/Assignee/Titel/PT → `useBoardStore.updateTask`; **Sprint ist Story-Ebene** → `useSprintStore.assignStory(storyId, sprintId|null)`, Tasks **ohne `storyId`** zeigen „–" (Sprint nicht editierbar), Sprint-Optionen aus den Projekt-Sprints. `board/page.tsx` reicht die neuen Props durch (`assignStory` aus dem Sprint-Store). Keine neue Dependency, keine Persist-Migration. Tests `tests/task-030/` + E2E `e2e/board.spec.ts` (Inline-Titel überlebt Reload). Task-Datei archiviert.
- 2026-06-15 · **TASK-031 – Tags/Labels für Backlog & Board** abgeschlossen (PR #53 → `dev`): `Tag { id, name, color }` + `BoardTask.tagIds?` (`src/types/index.ts`). Feste **Token-Farbpalette** (`primary|accent|info|success|warning|danger`) mit literalen Klassen in neu `src/lib/tags.ts` (`TAG_COLOR_STYLES`/`TAG_COLORS`, `bg-<token>/10`+`text-<token>` wie StatusBadge – keine Ad-hoc-Hex) + reine Helfer `tagsForIds` (Store-Reihenfolge, unbekannte Ids raus), `taskHasTag`, `detachTaskTags` (leeres `tagIds` wird ganz entfernt). Neu `useTagStore` (persist `pm-studio-tags`, CRUD); `removeTag` entkoppelt überall via neuer Board-Action `useBoardStore.detachTag` (Board-Persist v4→v5, `tagIds` additiv). UI: neu `TagChips` (Karte `max=3`/Liste `max=2`, `+N`-Überlauf in `bg-secondary`), neu `TagManager`-Dialog auf dem Board (Button „Tags verwalten": anlegen/umbenennen live/umfärben via Swatches/löschen), Tag-Toggle-Chips im `TaskDialog`, `board-rows` löst `tags` für die Liste auf (neuer optionaler `tags`-Param), `BoardListView` neue Spalte „Tags" (nicht sortierbar). Einwertiger Tag-Filter: `BoardFilter.tagId` in `board-filters.ts` + Control in board/page.tsx. ⚠️ **Wichtig:** `TaskCard` selektiert das **stabile** `state.tags`-Array und leitet via `useMemo` ab – ein Selektor, der `tagsForIds(...)` direkt zurückgibt, löst zustand-„getSnapshot should be cached"-Loop aus (in E2E gefangen). Tests `tests/task-031/`. Task-Datei archiviert.
- 2026-06-15 · **TASK-037 – Story → Aufgaben (Scrum-Hierarchie)** abgeschlossen (PR #51 → `dev`): Unter jeder Backlog-Story konkrete Aufgaben anlegen/verwalten. Dev-Aufgaben = **Board-Tasks mit `storyId`** (kein neues Entity, „eine Quelle"). Neu `src/lib/story-tasks.ts` (reine Helfer `tasksForStory` [sortiert Spalte→`order`], `storyTaskRollup` [Anzahl/erledigt + PT-Summen, `storyDone` via `isStoryDone` ⇒ konsistent zu TASK-021], `detachStoryTasks`). Neu `src/components/project/StoryTasks.tsx` je Story in `BacklogView`: Roll-up `done/total · donePt/totalPt PT`, Task-Liste (Spalten-Status, Assignee, PT, Priorität), Inline-Add-Form (Titel/Priorität/PT/optional Person → `addTask` Spalte `todo`, geerbte Story-Priorität), Empty-State, Link „Im Board ansehen". Board-Action `useBoardStore.detachStory`; `useProjectStore` entkoppelt Story-Tasks bei `removeIdea` (Projekt weg) + `setArtifacts` (nur die im neuen Backlog fehlenden Stories) – kein Verwaisen. Tests `tests/task-037/` (Helfer/Detach-Store/Komponente); `tests/task-007/BacklogView.test.tsx` auf `getAllByText("Hoch")` gelockert (Add-Form zeigt Default-Priorität). Task-Datei archiviert.
- 2026-06-15 · **TASK-029 – Einstellungen & Theme (Dark/Light/System)** abgeschlossen (PR #49 → `dev`): Theme Dark/Light/System (Dark Default). `globals.css`: `:root` = Light-, `.dark` = Dark-Palette (vollständige Light-Tokens, Status-Töne kräftiger). No-Flash: Inline-Skript `themeInitScript` + `applyThemeClass`/`resolveTheme` in `src/lib/theme.ts`, in `layout.tsx` eingehängt (`<html suppressHydrationWarning>`, kein hartkodiertes `dark`); `ThemeApplier` (`src/components/settings/`) hält Klasse synchron und folgt `system` live via `matchMedia`. Eigener `useSettingsStore` (persist `pm-studio-settings`, **keine** neue Dependency): `theme`/`defaultBoardView`/`motion`. `/settings`-Seite mit `SettingsSegment` (ARIA-Radiogroup) für die drei Optionen; Navigation-Gruppe „System". Default-Board-Ansicht greift über `viewExplicit` im `useBoardStore` (Persist v3→v4: Bestand = explizit). Reduced-Motion-Override in `useReducedMotion` (`reduced`/`full` erzwingen, `system` = Media-Query). Tests `tests/task-029/`. **Task-Datei archiviert.**
- 2026-06-15 · **TASK-028 – Sprint-Visualisierung (Balken)** abgeschlossen (PR #47 → `dev`): Neue wiederverwendbare `src/components/ui/ProgressBar.tsx` (`value/max/status?`, `role="progressbar"` + `aria-value*`, clamped auf `[0,max]`, `max<=0` ⇒ leer; Füllfarbe single-sourced über `STATUS_STYLES.dot`, Spur `bg-secondary`, Breiten-Transition mit `motion-reduce`). `SprintColumn`: Fortschritt (`donePt/plannedPt`) als grüner Balken unter der Punktezeile, Auslastung je Person als Kapazitätsbalken (`assignedPt/capacityPt`, Farbe via `loadStatus`), Überlast zeigt rote Prozentangabe. Berechnungen (`sprintProgress`/`sprintWorkload`/`loadStatus`) unverändert. Tests `tests/task-028/ProgressBar.test.tsx`. Task-Datei archiviert.
- 2026-06-15 · **TASK-027 – Board-UI-Überarbeitung** abgeschlossen (PR #45 → `dev`): Kanban-Spalten teilen sich die Breite (`flex-1`/Min-Breite, Horizontal-Scroll nur Fallback), interner Spalten-Scroll (`ScrollArea`), Spaltenkopf mit Status-Streifen + Count- **und** PT-Summen-Pille + WIP-Hinweis (`BOARD_WIP_LIMITS`), Karten-Feinschliff. Reine Helfer `sumEstimatePt`/`isWipExceeded` in `src/lib/board.ts`. Fix grau-auf-grau: `bg-secondary` statt `bg-muted`+`text-muted-foreground` (beide Tokens identisch). Task-Datei archiviert.
- 2026-06-15 · **TASK-026 – Echte Sprint-Burndown** abgeschlossen (PR #43 → `dev`): `BoardTask.doneAt?` (gesetzt/entfernt via `stampDone` in `useBoardStore`, Persist v2→v3 additiv), reine `sprintBurndown` in `src/lib/burndown.ts` (Ideal- vs. Ist-Linie, `null` für Zukunft, keine Doppelzählung), Chart `SprintBurndownChart` mit `subtitle`/`action` auf echte Daten + Sprint-Auswahl (Default aktiver Sprint), Demo-`burndown`/`BurndownPoint` entfernt. Task-Datei archiviert.
- 2026-06-15 · **TASK-025 – Releases & automatische Sprint-Generierung** abgeschlossen (PR #41 → `dev`): `Release`-Typ + `PlannedSprint.releaseId?`, `useReleaseStore` (persist `pm-studio-releases`, CRUD), reine `generateSprints`/`sprintWindows` in `src/lib/release.ts`, Sprint-Store `setReleaseSprints` (idempotent über `releaseId`) + `detachRelease`, Route `/releases` mit `ReleaseDialog`/`ReleaseCard`, Navigation. Task-Datei archiviert.
- 2026-06-15 · **TASK-024 – Sprint-Timeboxes & aktiver Sprint** abgeschlossen (PR #39 → `dev`): `PlannedSprint.startDate?/endDate?`, Datumsfelder + „Ende ≥ Start"-Validierung im `SprintDialog`, reine Helfer `isActiveSprint`/`formatSprintRange` in `src/lib/sprint.ts`, `SprintColumn` mit Zeitraum + „Aktiv"-Badge (SSR-sicher). Task-Datei archiviert.
- 2026-06-15 · Zwischenausbau „Team & Delivery-Tiefe" abgeschlossen: TASK-019, 020, 021, 022, 023 (alle gemergt).

## Als Nächstes (Reihenfolge: Logik zuerst)
> **Der gesamte UI-Backlog ist VOLLSTÄNDIG.** Milestone-Gruppe „5++++ Backlog als Modul"
> (056–062) **und** die UI-Backlog-Quick-Wins (063 Listen-Gruppierung, 064 Item-Keys,
> 065 Retro→Task, 066 UI-Sprache) sind erledigt. In `tasks/` liegen nur noch die
> **Agenten/Backend-Tasks** (010, 012–016). Kein Release fällig (v0.6.0 = M6 „erster
> echter Agent"; die UI-Wellen waren Teil von M5++++, kein eigenes Meilenstein-Tag).

1. **NÄCHSTER TECHNISCHER SCHRITT: Backend/M6 – größerer Kontextwechsel.** Einstieg **TASK-010 (erster lokaler Agent)** (`tasks/TASK-010-first-local-agent.md`), danach 012–016 (HITL-Step-Lifecycle, Artifact-Review-Panel, Workflow-Definitionen, Agent-Conversation-UI, Artifact-Versioning) – Weg zu **v0.6.0**. Erfordert **FastAPI + Ollama + LangGraph** (neuer Stack lt. CLAUDE.md „Backend später"). ⚠️ Vor Umsetzung die jeweilige Task-Datei **und** `docs/agent-system.md` lesen; das ist ein anderer Layer als die bisherige reine Next.js/Zustand-UI. Die Mock-Pipeline (`mock-agent-service.ts`/`agent-service.ts`, Guard `tests/task-009/architecture.test.ts`) bleibt bis der echte Agent steht.
4. **Wiederverwendbare Muster:** reine Helfer/Selektoren in `src/lib/` + Unit-Tests, Stores in `src/store/` (Store-Init-Zyklen vermeiden – vgl. Backlog-Migration liest `localStorage` direkt statt `useProjectStore` zu importieren), transaktionales Edit via Save-Patch + `onChange`/Store-Action (vgl. `setRisks`), Status-Farbe **nur** über `<StatusBadge>`, Safe-Delete via `useConfirmDelete` (Snapshot **aller** betroffenen Slices – Projekt-Delete snapshottet jetzt auch die Backlog-Slice), Listen via `BoardListView`/`FilterBar`, SSR via `useHydrated`-Gate, Detail-Route via `sprints/[id]`/`projects/[id]`, Inline-Anlegen via `QuickAddTask`/`AddColumnTile`. **Workflow:** `git checkout dev && git pull`, dann `git checkout -b feat/task-0XX-...`.
5. **Archivierung erledigt:** `tasks/` enthält nur noch offen 010, 012–016. Archivierung passiert **im selben Task-Branch/PR** (CLAUDE.md Workflow-Schritt 7).

## Architektur-Entscheidungen / offene Edge Cases
- TASK-065 (erledigt): **Retro-Maßnahme↔Task-Verknüpfung über ZWEI Felder** (`sourceRetroId` + `sourceRetroAction`), bewusst gegen die Task-Skizze (nur `sourceRetroId`): Retro-`actions` sind reine **Strings ohne Id**, eine Retro hat mehrere ⇒ nur (Retro-Id, Aktions-Text) identifiziert die einzelne Maßnahme und trägt den Duplikat-Schutz. Der Task-**Titel** ist editierbar, `sourceRetroAction` speichert den **Original-Text** ⇒ Verknüpfung bleibt stabil. **Store bewusst unverändert** (kein neues `addTaskFromRetro`): `buildRetroActionTask` liefert `Omit<BoardTask,"order">`, `addTask` vergibt `order` + Item-Key (TASK-064). „Erscheint sofort auf Board & My Work" ergibt sich automatisch (beide lesen den Board-Store; Spalte `todo`, `projectId` vom Sprint). **Mini-Dialog `RetroActionDialog` initialisiert eine keyed innere Form** aus Props (Muster `CeremonyDialog`), **nicht** via `useEffect`-Reset (sonst `react-hooks/set-state-in-effect`-Lint-Fehler). Dedup-Anzeige: `retroActionHasTask(retroId, action, boardTasks)` → Badge statt Button; **gelöschter Task** ⇒ Lookup wird wieder falsch ⇒ Button kommt zurück (robust, kein Absturz). `CeremonyHistory`-Props `boardTasks`/`onCreateActionTask` **optional** (Default `[]`/noop) ⇒ TASK-054/055-Aufrufer/-Tests bleiben grün. `SprintDetail`-Ceremonies-Sektion nutzt denselben reinen `ceremonyHistory`-Helfer (Filter auf `sprintId`), verlinkt pauschal zu `/ceremonies` (kein per-Eintrag-Deeplink im Projekt).
- TASK-064 (erledigt): **Item-Keys leben in einem eigenen Leaf-Store (`useItemKeyStore`) als zentrale Karte `keys[itemId]`**, nicht als Feld auf Story/Task. Das war die entscheidende Design-Wahl: (a) **keine Persist-Migration** auf `useBacklogStore`/`useBoardStore` nötig, (b) **kein** neuer Store-Init-Zyklus (der Leaf importiert nichts; die Domänen-Stores rufen ihn nur zur Laufzeit via `getState()` – exakt das `logActivity`-Muster), (c) **undo-stabil**, weil Keys **nie** aus der Karte gelöscht werden (eine gelöschte + per Undo wiederhergestellte Story findet ihren alten Key wieder). **Eine gemeinsame Laufnummer je Projekt** über Stories **und** Tasks (Jira: `PMS-1` Story, `PMS-2` Task). **Counter zählt nur hoch** (auch nach Löschen ⇒ nie Recycling); Prefix + Counter werden bei Projekt-Löschung **nicht** aufgeräumt (bewusst, hält Keys undo-stabil; harmlos im lokalen Single-User). **Vergabe zentral in den Create-Actions** (`assignKey` außerhalb `set`, idempotent per Item-Id); **Prefix-Registrierung** garantiert über `addIdea` (neu) + `runBackfill` (Bestand), Lazy-Fallback in `assignKey` falls doch mal keiner da ist. **Bestand-Backfill liest die drei Stores direkt aus `localStorage`** (zyklusfrei, hydrationsreihenfolge-unabhängig) und keyt einmalig in Erstellungsreihenfolge-Näherung (**Stories nach `rank`, dann Tasks nach `column`/`order`** – Stories/Tasks tragen keinen `createdAt`, daher Näherung, dokumentiert). ⚠️ **zustand-Falle:** Key in Komponenten als **primitiven** Selektor `state.keys[id]` lesen (String|undefined, stabil) – **nicht** eine abgeleitete Struktur zurückgeben. **Prefix-Ableitung** (`derivePrefix`): Initialen bei mehreren Wörtern, erste 3 Buchstaben bei einem Wort, Akzent-Strip via `normalize("NFD")`, `PRJ`-Fallback, **deterministischer Zahlensuffix** bei Kollision (`APS`→`APS2`). **Suche:** exakter Key-Match rankt zuerst; `keys` in `SearchSources` optional (Altaufrufer/Tests bleiben gültig). **Bewusst offen:** Prefix-**Editieren** (Task nannte „editierbar", aber nur in der Betroffene-Dateien-Skizze, kein AC) – `useItemKeyStore` könnte leicht ein `setPrefix` bekommen; UI dafür ist Future Scope.
- TASK-062 (erledigt): **Release-Scope = `UserStory.releaseId`** (einwertig, optional). Zwei **bewusst getrennte** Scope-Begriffe: **Content-Scope** (Stories via `releaseId`, `releaseScope` in `release.ts`, TASK-062) vs. **Sprint-Fortschritt** (Stories in den Release-Sprints, `releaseProgress`, TASK-041) – auf der `ReleaseCard` beide Zeilen (neu „Scope:", alt umbenannt „Sprints:"). **Done nur über `isStoryDone`** (TASK-038), nie zweite Logik. **Zuordnen** an zwei UIs, **eine** Action `updateStory({ releaseId })` (Dialog-Select + inline `EditableSelectCell`); Sentinel **`NO_RELEASE = "__none__"`** (base-ui Select braucht String, leer unzulässig) sowohl fürs Zuordnen (= `undefined`) als auch als Filter-Wert „Ohne Release". **Fußzeile folgt dem Scope automatisch**, weil `backlogSummary` über die bereits gefilterte Story-Liste summiert (keine separate Scope-Summe). **Kaskade** `useReleaseStore.removeRelease → useBacklogStore.detachRelease` (Muster `detachTag`); **kein Import-Zyklus** (Backlog-Store importiert **nicht** Release-Store, nur umgekehrt zur Laufzeit via `getState()`). ⚠️ **Release-Löschen-Undo muss zusätzlich die Backlog-Slice restaurieren** (`releases/page.tsx` snapshottet releases+sprints+**backlog**), sonst käme die `releaseId`-Zuordnung nach Undo nicht zurück. **Inline-Zelle disabled ohne Releases** (`releaseOptions.length <= 1`, nur der `–`-Eintrag). **Bewusst offen (Notiz `docs/future-scope.md`):** Burnup-Chart & Velocity-Forecast – `releaseScope` ist die Grundlage, eigener Task.
- TASK-061 (erledigt): **Risiken sind jetzt eine erstklassige Store-Collection** (`useRiskStore`, `pm-studio-risks`) statt Artefakt-Feld – existieren ohne Pipeline, `importRisks` ist additiv/idempotent über Risiko-Id (Muster `importBacklog`), Bestand wird einmalig aus `pm-studio-projects`-localStorage übernommen (zyklusfrei, kein `useProjectStore`-Import). Das **Artefakt bleibt Snapshot** (Pipeline schreibt weiter `artifacts.risks`, aber UI/Dashboard/OverviewTab lesen den Store). `RiskTable`-API blieb (Array + `onChange`); nur die Persist-Quelle wurde getauscht (`useProjectStore.setRisks` entfernt). **Projekt-Detail** rendert kein Backlog mehr, sondern eine **`BacklogReferenceCard`** (Rollup + Deep-Link) → „eine Quelle". ⚠️ **zustand-Falle:** Risiken je Projekt als stabile Slice `state.risks[id]` selektieren, `?? []` **im Render** (nicht im Selektor). **Bewusst offen:** `BacklogView.tsx`/`StoryTasks.tsx` bleiben physisch (weiter getestet), nur nicht mehr im Projekt-Detail gerendert – physisches Entfernen wäre separater Refactor.
- TASK-059 (erledigt): **`/sprints` ist jetzt ein vertikales, ein-/ausklappbares Sektionslayout** (`SprintSection`) statt horizontaler `SprintColumn`-Scroll – skaliert mit vielen Sprints, Backlog-Sektion zuletzt. **`SprintColumn` und die `.thin-scrollbar`-Utility (TASK-053) sind entfernt** (nur hier genutzt). Sektionen sind per Default **offen**; eine **eingeklappte** Sektion rendert ihren Body (Story-Liste = Droppable) nicht ⇒ man kann nicht in eine zugeklappte Sektion droppen (Standardverhalten, akzeptiert). **Commitment-Warnung** ist ein **reiner** Helfer `sprintCommitment(plannedPt, persons)` (`capacity.ts`): Σ Kapazität der übergebenen Personen (heute **ganzer Roster** = „Team", da Sprints kein Team-Feld haben); **`capacityPt > 0`-Guard** ⇒ ohne Kapazität **nie** Warnen (leerer Roster flaggt sonst jeden Sprint). Fortschritt/Done weiterhin **nur** über `sprintProgress`/`isStoryDone` (TASK-038), **keine** zweite Logik. ⚠️ **Test-Isolation:** `userEvent.click` auf den dnd-Griff startet in jsdom einen echten dnd-kit-Drag (Default-`PointerSensor` aktiviert auf `pointerdown` – die Test-`DndContext` setzt **keine** `activationConstraint`); dessen Reststate stört den **unmittelbar folgenden** Test (Toggle-`onClick` feuert nicht). ⇒ Griff-Klicks in Unit-Tests via **`fireEvent.click`** (reines Click-Event, kein Pointer-Sequence ⇒ kein Drag) – testet die `stopPropagation` des Griffs trotzdem. **Bewusst offen:** Rank-D&D **innerhalb** der Backlog-Sektion nutzt weiter `assignStory`-Semantik über die bestehende `handleDragEnd` (kein separater Rank-Pfad ergänzt – die AC „Rank in Backlog" ist über die vorhandene `reorderStory`-Basis erfüllbar, aber die Sprint-Seite ruft sie nicht selbst; Reorder passiert im `/backlog`-Bereich, TASK-057).
- TASK-058 (erledigt): **`StoryDialog` ist die einzige Story-Bearbeitungsquelle** und **self-contained** (liest Stores + `useConfirmDelete` selbst, Props nur `story`+`onOpenChange`) ⇒ triviales Andocken überall. **Zwei AK-Familien bewusst getrennt:** Store-`UserStory.acceptance_criteria` ist jetzt **abhakbar** (`AcceptanceCriterion[]`), das Agenten-Artefakt **`BacklogStory` bleibt `string[]`** (Run-Snapshot) – `flattenBacklog`/Import mappt über `criteriaFromStrings`. **`normalizeAcceptance` gibt IMMER ein Array** (AK ist Pflichtfeld, anders als die optionale Checkliste → nie `undefined`). **AKs transaktional** über das Save-Patch (`updateStory({ acceptance_criteria })`), keine eigenen Store-Actions – konsistent mit Checkliste/Tags. ⚠️ **Persist-Migration v1→v2 ist Pflicht** (Bump ohne migrate verwürfe das Backlog): `migrateBacklogPersistedState` (exportiert ⇒ unit-testbar) hebt String-AKs verlustfrei; tolerant ggü. bereits gehobenen Einträgen. **Epic-Verschieben = eigene Action `moveStoryToEpic`** (nicht über `updateStory`, das `epicId` bewusst aus dem Patch ausschließt): rankt ans **Ende** des Ziel-Epics, hebt `agent`→`human_edited`. **Activity-Granularität (TASK-043):** create/update/delete **und** move loggen (`entityType:"story"`), aber **`reorderStory` NICHT** (reines Ranking); Cascade-Deletes via `removeEpic`/`removeProjectItems` laufen `set`-direkt (kein `removeStory`) ⇒ auch kein Log – bewusst. ⚠️ **Klick-vs-Drag:** in der Sprint-Planung trägt **nur der Grip-Handle** die dnd-`listeners` (Kartenkörper-`onClick` öffnet den Dialog); im Backlog stoppen Inline-Zellen/Griff/Löschen `stopPropagation`, damit der Zeilenklick öffnet statt zu editieren. ⚠️ **E2E-Falle:** Playwright `getByLabel(\"Titel\")` matcht **substring** (auch „Aufgabentitel\" aus der eingebetteten `StoryTasks`) ⇒ `{ exact: true }`; jsdom-`getByLabelText` matcht exakt (kein Konflikt). **Bewusst offen:** Tags im Dialog (Stories haben kein Tag-Feld, TASK-031); My-Work-Andockpunkt (die Task nannte ihn, aber My-Work zeigt Board-Tasks, keine Stories – kein sinnvoller Story-Klick dort).
- TASK-057 (erledigt): **`/backlog` ist die erste UI auf dem `useBacklogStore`** und rein präsentational + Store-verdrahtet (keine neue Logik-Schicht außer `backlogSummary`). ⚠️ **Verschachtelte Buttons vermeiden:** der Accordion-Collapse-Toggle ist ein **eigener** kleiner `AccordionTrigger`, damit inline-Titel (`EditableTextCell`) und Löschen **Geschwister** bleiben (ein `<button>` im `<button>` ist invalides HTML + bricht Klicks). ⚠️ **dnd nur am Griff:** nur `GripVertical` trägt `attributes`/`listeners`, sonst wären die Inline-Zellen nicht mehr klickbar; Rank-D&D ist **auf ein Epic beschränkt** (`handleDragEnd` bricht ab, wenn `over` in einem anderen Epic liegt – eine Story behält ihr Epic). **Done-Aggregat** kommt **ausschließlich** aus `storyTaskRollup`/`isStoryDone` (TASK-038) via neuem reinen `backlogSummary` – nie zweite Logik. **Undo-Snapshot umfasst Backlog- + Board-Slice**, weil Story-/Epic-Delete Board-Tasks detacht (nur Backlog-Restore würde die `storyId`-Verknüpfung nicht zurückholen). **CommandBar-Intent `new-story`** öffnet das Add-Feld **deklarativ** (Muster TASK-042/Release: `addStoryEpicId ?? (wantNewStory ? firstEpic : null)`, Schließen `clearIntent()`) – **kein setState-in-Effect**. **Deep-Link `?project=`** wird beim Mount aus `window.location.search` gelesen (kein `useSearchParams` ⇒ keine Suspense-Pflicht; SSR-sicher, weil bis zum `useHydrated`-Gate „Lädt …" gerendert wird). ⚠️ **Bewusst NICHT umgesetzt:** Tag-Filter (Stories haben kein Tag-Feld; Tags = Board-Aufgaben, TASK-031) und Row-Click-Story-Dialog (TASK-058). ⚠️ **CI-Lehre:** `tsc --noEmit` prüft Testdateien mit – `vi.fn()`-Mocks **direkt** als Props übergeben, nicht über ein `ReturnType<typeof vi.fn>`-getyptes Objekt (nicht zuweisbar). **Vor dem ersten Push die neu angelegten Testdateien in den lokalen `tsc`-Lauf einschließen** (sonst wird der Fehler erst in CI sichtbar).
- TASK-056 (erledigt): **Backlog-Store ist die Quelle der Wahrheit**, das Agenten-Artefakt (`ProjectArtifacts.backlog`) nur noch ein **Snapshot des Laufs**. ⚠️ **Zwei Typ-Familien bewusst getrennt:** Artefakt bleibt **nested** (`BacklogEpic`/`BacklogStory`), Store ist **flach** (`Epic`/`UserStory` mit `epicId`/`rank`); `flattenBacklog` überführt Snapshot→Store und erhält **Story-Ids 1:1** (⇒ Board-/Sprint-`storyId` gültig, **keine** Persist-Migration dort). **`importBacklog` ist additiv & idempotent** über die Artefakt-Story-Id: ein Re-Run fügt nur neue Items hinzu, überschreibt (ggf. editierte) Bestandsitems **nie** – deshalb detacht `setArtifacts` auch **nicht mehr** (das alte „regenerieren ersetzt Stories"-Verhalten ist weg). ⚠️ **Kein Store-Init-Zyklus:** `useBacklogStore` importiert **nicht** `useProjectStore` (das importiert seinerseits den Backlog-Store für die `removeIdea`-Kaskade). Die **einmalige Migration** liest die Alt-Artefakte darum **direkt aus `localStorage`** (`pm-studio-projects`, JSON `.state.artifacts`) in einem `queueMicrotask` in `onRehydrateStorage` – zyklusfrei **und** unabhängig von der Hydrationsreihenfolge; `migrateFromArtifacts(map)` selbst ist rein/testbar, das Flag `artifactsMigrated` verhindert Doppelimport (ein zweiter Aufruf ist No-op). **`updateStory` markiert `agent`→`human_edited`** (expliziter provenance-Patch gewinnt) → Grundlage für die Vorschlags-Inbox (TASK-060). **Kein Activity-Log** für Backlog-Änderungen (`ActivityEntityType` kennt kein epic/story; Granularität wie TASK-043/045; falls später gewünscht: Typ erweitern + `logActivity` außerhalb `set`). **Sprint-Vorschläge (`sprint_suggestions`) bleiben am Artefakt** (Import über `importSuggestions` unverändert). ⚠️ Für **TASK-057+**: CRUD/Reorder-Actions + `src/lib/backlog.ts`-Selektoren sind vorhanden; `UserStory.releaseId`/`sourceRequirementIds`/`description` existieren bereits als (optionale) Felder für 062/058. Projekt-Delete-Undo snapshottet jetzt zusätzlich die **Backlog-Slice** (`useBacklogStore.restore`).
- TASK-055 (erledigt): **Verlinken + Kommentieren passiert am Historien-Eintrag** (`CeremonyEnrichment`), **nicht** im TASK-054-Anlege-Dialog – der bleibt unverändert (minimales Regressionsrisiko, E2E-Seed-Fragilität aus TODO beachtet). Neue Felder (`linkedStoryIds`/`linkedTaskIds`/`comments`) hängen an **`CeremonyMeta`**, das Review **und** Retro teilen ⇒ eine Store-Action-Familie deckt beide via generischem `patchEntry(state, kind, entryId, fn)` ab. ⚠️ **Alle Felder optional ⇒ keine Persist-Version-Erhöhung** (v2 bleibt; additiv). Ein Bump ohne `migrate` würde die Ceremonies verwerfen – hier bewusst **nicht** gebumpt. **Kein per-Item-Deeplink** existiert im Projekt → verlinkte Items (Story wie Task) navigieren zur **Sprint-Detailseite `/sprints/<sprintId>`** (TASK-053), wo genau diese Items gezeigt werden. **Verwaiste Links** (gelöschte Story/Task) werden über `resolveLinks` als `missing` markiert und als durchgestrichenes „Gelöschtes Item" **ohne Anchor** gerendert – nie bereinigt (Historie bleibt ehrlich), bricht die Ansicht nicht. **Kommentare/Links werden NICHT geloggt** (Activity-Granularität gegen Rauschen, wie TASK-043/045). `CeremonyHistory`-Props sind **optional** (Default: leere Items + No-op-Callbacks) ⇒ TASK-054-Tests bleiben grün. Linkbare Tasks = Board-Tasks mit `storyId ∈ sprint.storyIds` (aus `useBoardStore`); Stories aus `artifacts[projectId].backlog`.
- TASK-054 (erledigt): **Ceremonies = historische Liste**, nicht Upsert. Der Store hält Reviews/Retros append-only (`addReview`/`addRetro`); `removeSprint` räumt weiterhin alle Einträge des Sprints ab. **Content und Meta sind bewusst getrennt:** die Form-Helfer `reviewFromForm`/`retroFromLists` liefern nur `ReviewContent`/`RetroContent` (Body **unverändert** – so blieben TASK-018-Schema-Tests grün), die Ceremony-Metadaten (id/scope/teamId/createdAt) kommen ausschließlich aus **`src/lib/ceremonies.ts`** `createReview`/`createRetro` (id/now injizierbar ⇒ deterministische Tests). **`teamId` nur bei `scope==="team"`** (makeMeta droppt es sonst ⇒ kein Dangling). ⚠️ **Persist-Migration v1→v2 ist Pflicht** (Bump ohne `migrate` verwürfe den Bestand): `backfillCeremony` setzt fehlende id/scope/`createdAt` (Epoch → UI „Datum unbekannt"), tolerant ggü. fehlenden Arrays, No-op für v2. Neue Done-/Datum-Logik gibt es **nicht** – geplante PT bleiben abgeleitet (aus `artifacts` je Sprint). Statusfarbe nur über `<StatusBadge>` (Review=info/Retro=success), **Scope ist kein Status** → neutraler `bg-secondary`-Chip. `today`/Datum erst nach `useHydrated`-Gate. **Klemmbrett-Flow auf `/sprints` bleibt** und legt jetzt append-Einträge mit `scope:"project"` an (kein Prefill mehr = „neuer Eintrag"). E2E: Sprint im Dialog **vorausgewählt** + Scope-Default `cross` ⇒ Formular rendert sofort (kein base-ui-Select-Klick im Happy-Path nötig). **TASK-055 (v2)** baut auf `/ceremonies` + `ceremonies.ts` auf (verlinkte Items + Kommentare).
- TASK-053 (erledigt): **Sprint-Detailseite = eigene Route** (`/sprints/[id]`), nicht nur ein Panel – Server-Component reicht `id` an die Client-`SprintDetail` durch (Muster `projects/[id]`). Fortschritt/Done kommen **ausschließlich** aus den bestehenden Quellen `sprintProgress`/`sprintBurndown`/`isStoryDone` (TASK-038) – **nie** eine zweite Done-Logik. Die Story-/Task-Liste rendert dedizierte klickbare Task-Buttons (nicht `StoryTasks`, das hat Add-Form + öffnet keinen Dialog) und öffnet denselben `TaskDialog` wie das Board (`updateTask`/`useConfirmDelete`). `today` (für Burndown + „Aktiv"-Badge) **nur nach dem `useHydrated`-Gate** berechnen (SSR-sicher). Navigation über `detailHref`-Prop an `SprintColumn` (nur reale Sprints, nicht die „Nicht zugeordnet"-Spalte). **Scrollbar-Fix = CSS-Utility `.thin-scrollbar`** (globals.css, `scrollbar-color`/`::-webkit-scrollbar` aus Border-Token) statt DnD-riskantem `ScrollArea`-Wrap – themed statt weiß, ohne die dnd-kit-Autoscroll zu stören. ⚠️ **E2E-Seed-Falle:** `e2e/sprints.spec.ts` Projekt-Seed muss **Version 2** sein **und `risks:{risks:[]}`** enthalten, sonst crasht die v1→v2-Risk-Migration (TASK-045) und die Seite zeigt „Noch keine Projekte". **Dieselbe Falle steckt noch in `e2e/my-work.spec.ts` (offen).**
- TASK-045 (erledigt): **Risiken sind ein gepflegtes Artefakt-Feld**, kein eigenes Entity – Bearbeiten läuft **transaktional** über das Save-Patch des Projekt-Artefakts (`useProjectStore.setRisks`, ersetzt `artifacts[ideaId].risks.risks` 1:1; **no-op ohne Artefakte**, Risiken existieren erst nach Pipeline-Lauf). `RiskTable` hält **keine** Logik, nur Dialog-State + reicht das **gesamte** nächste Array via `onChange` durch (Muster wie Checklisten/Tags, Abbrechen verwirft). **Status-Farbe ausschließlich** über `RISK_STATUS`→`<StatusBadge>` (nie Ad-hoc). ⚠️ **Migration v1→v2 ist Pflicht**, weil `status` jetzt required ist: `withRiskStatus` backfillt Altbestand auf „open" (`risk.status ?? DEFAULT` statt `{status, ...risk}` – sonst TS-„specified more than once"). Generierung (`mock-templates`) setzt `status` direkt. ⚠️ **Risiko-Änderungen werden NICHT geloggt** (Activity-Granularität gegen Rauschen, TASK-043) – falls später gewünscht: `logActivity` in `setRisks` außerhalb des `set`-Updaters. Löschen via `useConfirmDelete` mit **Array-Snapshot-Undo** (kein Store-`restore` nötig, da `onChange(before)` reicht). `createRisk`-id ist injizierbar (deterministische Tests).
- TASK-043 (erledigt): **Activity-Log ist append-only und wird zentral über genau einen Pfad geschrieben** (`useActivityStore.log`/`logActivity`). Der Store ist bewusst ein **Leaf** (kein Import anderer Stores) – nur so dürfen die Domänen-Stores ihn aus ihren Actions heraus aufrufen, ohne Modul-Init-Zyklus; der Aufruf passiert **außerhalb** des `set`-Updaters (Updater bleiben rein), Namen/Titel fürs Lösch-Event kommen via `get()` **vor** dem `set`. **Granularität ist die getroffene Designentscheidung** (Offener Punkt der Task): nur create/delete + **bedeutsame** Updates (Spaltenwechsel/Sprint-/Release-Update) – Reorder, reine Feldedits und Cascade-Detaches **nie** loggen, sonst Rauschen. ⚠️ **Undo (TASK-040) hebt Lösch-Events nicht auf** – der Log ist Historie (gelöscht → ggf. wiederhergestellt), bewusst nicht in Snapshot/Restore eingebunden. ⚠️ Events derselben Millisekunde teilen `createdAt`; „neueste zuerst" ist ein **stabiler** Sort ⇒ Gleichstände behalten Einfügereihenfolge (Store-Tests prüfen daher die Menge, die strikte Ordnung deckt `activity.test.ts` mit distinkten Zeiten ab). ⚠️ Panels: das Store-Array **stabil** selektieren und Slice via `useMemo` ableiten – `eventsForEntity(...)` direkt aus dem Selektor zurückzugeben löst den zustand-„getSnapshot"-Loop aus (vgl. TASK-031). `review`/`run`-Kinds reserviert für HITL (TASK-012/013).
- TASK-042 (erledigt): **My Work** ist eine **abgeleitete** Sicht – kein „mine"-Feld am Task; alles über `assigneeId` + reine Selektoren (`src/lib/my-work.ts`). **Überfällig kommt ausschließlich aus `due.ts`** (`isTaskOverdue` ruft `isOverdue`), Done aus `terminalColumnIds` (TASK-032) – **nie** zweite Logik. **Person-Auflösung gekapselt** in `resolveMyWorkPersonId(preferredId, persons)`: der spätere Login setzt den eingeloggten User als `preferredId` ein, die Seite bleibt unverändert. `today` SSR-sicher erst nach `useHydrated`-Gate; ohne `today` ⇒ 0 overdue (kein Crash). **Globale Suche** (`searchEntities`, `src/lib/search.ts`) ist indexfrei (Substring), pro Art gekappt; `CommandBar` läuft mit `shouldFilter={false}` + kontrollierter Query (cmdk filtert nicht selbst). ⚠️ **Create-Aktionen über Intent-Store, nicht über Effekt:** „Neue Person/Release" navigieren + setzen `useCommandActionStore` (ephemer); die Zielseite leitet `dialogOpen` **deklarativ** aus dem Intent ab und leert ihn in `onOpenChange` – ein `setState` im `useEffect` würde `react-hooks/set-state-in-effect` verletzen (Lint-Fehler). Edge: „Neues Release" ohne Projekt öffnet keinen Dialog, der Intent bleibt dann gesetzt (harmlos im lokalen Single-User-Kontext). E2E-Hinweis: CommandBar-Tests starten auf `/board` (die Dashboard-Startseite `/` löst den Such-Button durch Recharts-Remounts im Dev kurz aus dem DOM); Palette via `data-testid="command-trigger"` öffnen, nicht via Strg+K.
- TASK-041 (erledigt): Release-**Fortschritt** ist eine **reine, abgeleitete** Größe (`releaseProgress`) – **nicht** persistiert und **nicht** aus `Release.status` abgeleitet; nur der **Status** ist ein gepflegtes Feld. Done-Semantik kommt **ausschließlich** aus TASK-038 (`releaseProgress` → `sprintProgress` → `isStoryDone`); **nie** eine zweite `.some/.every`-/`column==="done"`-Logik einführen. Aggregation summiert nur über `sprint.releaseId === releaseId`; da eine Story in ≤1 Sprint lebt, kein Doppelzählen. Persist **v1→v2** ist **additiv** mit `migrate` (Altbestand ⇒ `status:"planned"`); ein Version-Bump **ohne** `migrate` würde den Release-Bestand verwerfen. Statusfarbe nur über `<StatusBadge>`/`RELEASE_STATUS` (eine Quelle). `today` für die Timeline-Aktiv-Markierung wird **nie** im Modul-/SSR-Scope berechnet, sondern erst nach dem `useHydrated`-Gate in `releases/page.tsx` (Muster wie sprints/board). ⚠️ Für Custom-Phasen (TASK-032) weiterhin `terminalColumns` durchreichen (Default `DEFAULT_TERMINAL_COLUMN_IDS`).
- TASK-040 (erledigt): **Eine** Confirm/Undo/Toast-Mechanik (`useConfirmStore`/`ConfirmDialog`, `useToastStore`/`Toaster`, Hook `useConfirmDelete`) – neue destruktive Aktionen **immer** über `useConfirmDelete` führen, nie roh löschen. **Undo-Strategie = Full-Slice-Snapshot + `restore`:** bewusst die ganze betroffene Slice ersetzen (1:1, inkl. Cascade), nicht chirurgisch re-inserten. ⚠️ Trade-off: parallele Änderungen im ~8-s-Undo-Fenster würden überschrieben – akzeptabel im lokalen Single-User-Kontext. Snapshot wird **im Handler vor dem Löschen** via `getState()` gezogen (kein stale-Closure). **Referenz-Aufräumung** beim Löschen ist Pflicht und folgt dem Tag-Muster: neuer dangling-ref ⇒ reiner `detach*`-Helfer in `src/lib/` + Board-Action + Aufruf im Owner-Store (`removePerson`→`detachAssignee`). Confirm ist ein **imperatives Promise** (Resolver modul-seitig, nicht im State); ein neuer `confirm()` verdrängt den offenen (alter ⇒ `false`). Toast-Auto-Dismiss liegt in der Component (`setTimeout`), Store bleibt timer-frei/SSR-sicher.
- TASK-036 (erledigt): Swimlanes sind **abgeleitete Ansicht** – kein Lane-Feld am Task; `buildSwimlanes` gruppiert nur. **Tag-Lanes = erster Tag** (`tagIds[0]`): bewusst, damit jeder Task in **genau einer** Lane liegt (sonst doppelte dnd-Ids derselben `task.id` in mehreren `SortableContext`). Lane-übergreifendes D&D ändert Gruppe nur für Assignee (`updateTask`) und Sprint (`assignStory`, **nur** wenn `storyId` vorhanden – Sprint hängt an der Story); **Tag-Drag ändert nur die Spalte** (Mehrfach-Tags sonst zerstört → im `TaskDialog` pflegen). ⚠️ D&D läuft über das Drag-**`data`** (`{type,columnId,laneId}`) auf Droppable/Sortable statt Id-Parsing – flache Ansicht setzt **kein** `laneId` und behält die bare Spalten-Ids/testids (E2E grün); Swimlane-Spalten sind per `<laneId>__<colId>` namespaced. `order` bleibt **global pro Spalte** (lane-unabhängig); Lanes filtern nur – Einfügen vor `beforeId` erhält die sichtbare Reihenfolge. Quick-Add nur in der flachen Ansicht. Keine leeren Lanes (nur Gruppen mit ≥1 Task); `groupBy` persistiert (Board-Store v7→v8 additiv, Default `none`).
- TASK-032 (erledigt): **Phasen sind Daten** im `useBoardColumnsStore` – einzige Quelle für Reihenfolge/Label/Status/WIP; `src/lib/board.ts` hält nur noch `DEFAULT_BOARD_COLUMNS` + Helfer. **Done = `isTerminal`** (nicht der Spaltenname): alle Kennzahlen leiten „erledigt" über `terminalColumnIds(columns)`/`isTerminalColumn` ab; Helfer defaulten auf `DEFAULT_TERMINAL_COLUMN_IDS` (`{done}`). ⚠️ **Store-Zyklus bewusst:** `useBoardColumnsStore` importiert `useBoardStore` (Tasks bei `removeColumn` umziehen) und `useBoardStore` importiert `useBoardColumnsStore` (terminale Ids für `stampDone`) – funktioniert, weil beide den jeweils anderen Store **nur zur Laufzeit** (`getState()` in Closures) nutzen, nie beim Modul-Init. ⚠️ Neue Helfer mit Done-Bezug **immer** `terminalColumns`-Parameter geben (Default `DEFAULT_TERMINAL_COLUMN_IDS`), nie `=== "done"`. ⚠️ Donut `selectTaskStatusDistribution` bucketet nach **Status-Akzent** der Phase (nicht über feste Spalten-Map) → robust gegen Custom-Ids. `addTask` in `StoryTasks`/`BacklogView` nutzt weiterhin literal `"todo"`/`"backlog"` als Startspalte (Default-Ids existieren immer); falls eine spätere Task das ändert, dort die erste (nicht-terminale) Phase aus dem Store nehmen.
- TASK-025 (erledigt): generierte Sprints hängen per `setReleaseSprints` hinter den manuellen (order = nextOrder + index); `sprintWindows` klemmt das letzte Fenster aufs Release-Ende; `generateSprints`-Ids sind random (Tests prüfen Fenster/Namen/Daten, nicht Ids). Release bearbeiten ⇒ Sprints werden ersetzt (re-generieren).
- TASK-026 (erledigt): `doneAt` wird nur durch Spaltenwechsel gepflegt – Altbestand ohne Stempel zählt in `sprintBurndown` als „bei Sprintstart erledigt" (sortiert über `""` vor jedes Datum). Burndown nutzt projektweit den aktiven Sprint als Default (sonst neuester mit Timebox), Stories aus `artifacts[projectId]`; Ist-Linie endet heute (`null` ab morgen). Bei Mehrfach-Done-Tasks je Story zählt der früheste `doneAt`.
- TASK-029 (erledigt): `:root` = Light, `.dark` = Dark in `globals.css`; `.dark`-Klasse zur Laufzeit (No-Flash-Skript vor Paint, `ThemeApplier` synchron, `system` live via `matchMedia`). Default-Board-Ansicht greift nur solange `viewExplicit=false` (Board-Store) – Bestand wurde per Persist v3→v4 auf `viewExplicit=true` gesetzt, folgt dem Settings-Default also bewusst nicht. ⚠️ Für TASK-032 (Custom-Phasen): `isTerminal` statt literal `done` auch in `stampDone`/Burndown/Velocity.
- TASK-037 (erledigt): Story-Tasks sind bewusst Board-Tasks mit `storyId` (kein eigenes Entity) – Andockpunkt für spätere Agenten/Testautomation. `storyTaskRollup` zählt Tasks/PT pro Story und meldet `storyDone` über `isStoryDone` (≥1 Task in `done`, TASK-021). Entkopplung läuft über `useProjectStore` (`removeIdea`/`setArtifacts`) → `useBoardStore.detachStory`; ⚠️ für TASK-032 (Custom-Phasen) auch hier `isTerminal` statt literal `done` berücksichtigen (Roll-up `doneTasks`/`donePt`).
- TASK-031 (erledigt): Tag-Farben sind eine **feste Token-Palette** (`src/lib/tags.ts`, `TAG_COLOR_STYLES` mit literalen Klassen) – neue Farben dort ergänzen, nirgends Hex. Löschen entkoppelt über `useTagStore.removeTag` → `useBoardStore.detachTag` (gleiches Muster wie Story-Detach). Tag-Filter ist **einwertig** (Membership), Mehrfach-Tags UND/ODER bewusst out of scope (FilterBar = Single-Select). ⚠️ **zustand-Selektor-Falle:** abgeleitete Arrays (`tagsForIds`) **nie** direkt im Selektor zurückgeben – stabiles `state.tags` selektieren und per `useMemo` ableiten (siehe `TaskCard`), sonst „getSnapshot should be cached"-Loop. ⚠️ Für TASK-032 (Custom-Phasen) hier nichts Spezielles; Tags sind spaltenunabhängig.
- TASK-034 (erledigt): „heute" wird **nie** im Modul-Scope/SSR berechnet, sondern erst nach dem Hydration-Gate in `board/page.tsx` (`hydrated ? toLocaleDateString("sv-SE") : ""`) und als Prop durchgereicht; `filterBoardTasks` ignoriert den Überfällig-Filter, solange `today` leer ist (no-op statt „alles weg"). `isOverdue`/`dueStatus` nehmen `done` als Parameter (heute `column === "done"`). ⚠️ Für TASK-032 (Custom-Phasen) hier `isTerminal` statt literal `done` einspeisen – sonst gelten Tasks in einer eigenen Done-Phase fälschlich als überfällig.
- TASK-035 (erledigt): Checklisten werden **transaktional** über das `TaskDialog`-Save-Patch persistiert (`checklist: normalizeChecklist(...)`), bewusst **ohne** eigene Store-Actions – Abbrechen verwirft, konsistent mit Tags/Fälligkeit. `renameChecklistItem` trimmt **nicht** (erlaubt leere Zwischenstände beim Tippen); die Bereinigung (Trim + leere raus + `undefined`) passiert zentral in `normalizeChecklist` beim Speichern. Checkliste ist **spaltenunabhängig** → für TASK-032 (Custom-Phasen) hier nichts zu beachten. Abgrenzung zu TASK-037 bleibt: Checklisten-Items sind **kein** eigenes Work-Item (nur Unterpunkte), Story→Aufgaben sind echte Board-Tasks.
- TASK-018 (erledigt): geplante PT werden **abgeleitet** (aus den zugeordneten Stories in `sprints/page.tsx`), **nicht** im Review gespeichert – so bleibt der Wert konsistent, wenn sich die Sprint-Zuordnung später ändert. Review/Retro sind **Upsert je Sprint** (kein Array-Append); `removeSprint` räumt sie mit auf. Persist-Version blieb **v1** (additive Felder, shallow-merge) – ein Version-Bump **ohne** `migrate` würde den persistierten Sprint-Bestand verwerfen. zod **v4**: Fehlermeldung über `{ error: … }`, nicht `invalid_type_error`. Einstieg nur für `active`/`done` (geplante Sprints haben kein sinnvolles Ergebnis).
- TASK-038 (erledigt): Story-Done = **alle Tasks terminal & ≥1 Task** (0 Tasks ⇒ offen). `isStoryDone` ist die **einzige** Quelle; alle Roll-ups/Kennzahlen konsumieren sie (nie eigene `.some/.every`-Logik einführen). ⚠️ Burndown: `storyDoneDate` nutzt jetzt das **späteste** `doneAt` der Story (Fertigstellung = letzter Task), nicht mehr das früheste. Für künftige Done-bezogene Helfer weiterhin `terminalColumns`-Parameter durchreichen (TASK-032).

## Gate-Reminder (vor jedem Push)
`npm run lint` · `npx tsc --noEmit` · `npm run test` · `npm run build` · (bei UI/Workflow) `npx playwright test`
Windows: vor dem Commit ggf. `git checkout -- package-lock.json` (swc-Drift).
