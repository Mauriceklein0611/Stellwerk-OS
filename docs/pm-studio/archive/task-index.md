---
module: pm-studio
type: doc
status: frozen
updated: 2026-07-08
imported: 2026-07-08
source: projectmind-os
---

> **Frozen** – importiert aus projectmind-os (Issue #10). Offene Alt-Tasks werden als [PMS]-Issues nachgezogen (separates Triage-Issue).

# Task-Index

| ID | Titel | Meilenstein | Status |
|---|---|---|---|
| TASK-001 | Projekt-Setup | M1 | done · archiviert |
| TASK-002 | Grundlayout & Sidebar | M1 | done · archiviert |
| TASK-003 | Dashboard-UI | M2 | done · archiviert |
| TASK-003b | Dashboard-Charts | M2 | done · archiviert |
| TASK-004 | Projektideen-Formular | M3 | done · archiviert |
| TASK-005 | Agenten-Karten & Übersicht | M4 | done · archiviert |
| TASK-006 | Workflow-Visualisierung | M4 | done · archiviert |
| TASK-007 | Projektdetailseite | M5 | done · archiviert |
| TASK-007b | Projektdetailseite – Redesign | M5 | done · archiviert |
| TASK-008 | Kanban-Board | M5 | done · archiviert |
| TASK-009 | Mock-Agenten-Outputs | M3 | done · archiviert |
| TASK-010 | Erster lokaler Agent (Ollama) | M6 | offen |
| TASK-011 | Dashboard – Live-Daten | nach M5 | done · archiviert |
| TASK-012 | HITL-Schritt-Lebenszyklus | M7 | offen |
| TASK-013 | Artefakt-Review-Panel | M7 | offen |
| TASK-014 | Workflow-Definitionen (Definition/Run) | M7 | offen |
| TASK-015 | Konversationale Agenten (Chat-UI) | M7 | offen |
| TASK-016 | Artefakt-Versionierung & Überarbeitung | M7 | offen |
| TASK-017 | Sprintübersicht & Sprint-Planung | M5 (Nacharbeit) | done · archiviert |
| TASK-018 | Retro- & Review-Formulare | M5 (Nacharbeit) | done · archiviert |
| TASK-019 | Team & Personen (Stammdaten) | 5+ Team & Delivery | done · archiviert |
| TASK-020 | Personen-Zuweisung & Auslastung | 5+ Team & Delivery | done · archiviert |
| TASK-021 | Done-Status & echte Velocity | 5+ Team & Delivery | done · archiviert |
| TASK-022 | Filter für Board & Sprintansicht | 5+ Team & Delivery | done · archiviert |
| TASK-023 | Board-Ansicht: Kanban & Scrum-Liste | 5+ Team & Delivery | done · archiviert |
| TASK-024 | Sprint-Timeboxes & aktiver Sprint | 5++ Releases & Scrum-Tiefe | done · archiviert |
| TASK-025 | Releases & automatische Sprint-Generierung | 5++ Releases & Scrum-Tiefe | done · archiviert |
| TASK-026 | Echte Sprint-Burndown | 5++ Releases & Scrum-Tiefe | done · archiviert |
| TASK-027 | Board-UI-Überarbeitung | 5++ Releases & Scrum-Tiefe | done · archiviert |
| TASK-028 | Sprint-Visualisierung (Balken) | 5++ Releases & Scrum-Tiefe | done · archiviert |
| TASK-029 | Einstellungen & Theme (Dark/Light/System) | 5++ Releases & Scrum-Tiefe | done · archiviert |
| TASK-030 | Inline-Editing der Listenansicht | 5+++ Board- & Backlog-Tiefe | done · archiviert |
| TASK-031 | Tags/Labels für Backlog & Board | 5+++ Board- & Backlog-Tiefe | done · archiviert |
| TASK-032 | Anpassbare Kanban-Phasen (032a/032b) | 5+++ Board- & Backlog-Tiefe | done · archiviert |
| TASK-033 | Quick-Add direkt in der Spalte | 5+++ Board- & Backlog-Tiefe | done · archiviert |
| TASK-034 | Fälligkeitsdaten & Überfällig-Hinweis | 5+++ Board- & Backlog-Tiefe | done · archiviert |
| TASK-035 | Checklisten/Subtasks auf Task-Ebene | 5+++ Board- & Backlog-Tiefe | done · archiviert |
| TASK-036 | Swimlanes (Gruppierung im Board) | 5+++ Board- & Backlog-Tiefe | done · archiviert |
| TASK-037 | Story → Aufgaben (Scrum-Hierarchie) | 5+++ Board- & Backlog-Tiefe | done · archiviert |
| TASK-038 | Story-Done-Semantik korrigieren (alle Story-Tasks terminal) | Future Scope (PM-Korrektheit) | done · archiviert |
| TASK-039 | Sprint Review & Retro | — | erledigt durch TASK-018 (PR #69) |
| TASK-040 | Sichere Lifecycle-Aktionen (Confirm/Undo/Toast) | Future Scope (PM-Korrektheit) | done · archiviert |
| TASK-041 | Release-Fortschritt & Timeline | Future Scope (PM-Korrektheit) | done · archiviert |
| TASK-042 | My Work & globale Suche | Future Scope (PM-Korrektheit) | done · archiviert |
| TASK-043 | Activity Feed & Decision Log | Future Scope (Nachvollziehbarkeit) | done · archiviert |
| TASK-045 | Editable Risk Register | Future Scope (Nachvollziehbarkeit) | done · archiviert |
| TASK-051 | „+ Spalte"-Kachel am Board (Quick Win) | UI-Backlog | done · archiviert |
| TASK-052 | Story-Aufgaben auffindbar machen (Discoverability) | UI-Backlog | done · archiviert |
| TASK-053 | Sprint-Detailseite | UI-Backlog | done · archiviert |
| TASK-054 | Ceremonies-Bereich v1 (Review/Retro eigenständig) | UI-Backlog | done · archiviert |
| TASK-055 | Ceremonies-Bereich v2 (verlinkte Items + Kommentare) | UI-Backlog | done · archiviert |
| TASK-056 | Backlog-Entities – Epic & Story als Store-Entitäten | 5++++ Backlog als Modul | done · archiviert |
| TASK-057 | Backlog-Bereich `/backlog` unter Delivery | 5++++ Backlog als Modul | done · archiviert |
| TASK-058 | Story-Dialog / Item-Detail (eine Komponente für überall) | 5++++ Backlog als Modul | done · archiviert |
| TASK-059 | Sprint-Planung v2 – vertikales Layout mit klickbaren Items | 5++++ Backlog als Modul | done · archiviert |
| TASK-060 | Agenten-Panel im Backlog (Mockup) – Chat + Vorschlags-Inbox | 5++++ Backlog als Modul | done · archiviert |
| TASK-061 | Projekt-Detail entschlacken + Risiken als Entitäten | 5++++ Backlog als Modul | done · archiviert |
| TASK-062 | Release-Scope – Items einem Release zuordnen + Scope-Filter | 5++++ Backlog als Modul | done · archiviert |
| TASK-063 | Board-Listenansicht im Octane-Stil (Gruppieren + Aggregat-Fußzeile) | UI-Backlog | done · archiviert |
| TASK-064 | Lesbare Item-Keys (PROJ-123) | UI-Backlog | done · archiviert |
| TASK-065 | Retro-Maßnahmen als Tasks (Action Items schließen den Zyklus) | UI-Backlog | done · archiviert |
| TASK-066 | UI-Sprache konsequent vereinheitlichen (Deutsch) | UI-Backlog | done · archiviert |

Status-Werte: offen / in Arbeit / Review / done. Nach Abschluss: Datum + Branch/PR-Link ergänzen.
**Abgeschlossene Tasks** werden vollständig in [tasks-archive.md](tasks-archive.md) archiviert; ihre Dateien in `tasks/` werden entfernt (Standard ab 2026-06-15).

## Verlauf

- 2026-07-06 · TASK-065 → **done** · Branch `feat/task-065-retro-action-tasks` · PR nach `dev` · **Retro-Maßnahmen als Tasks (Action Items schließen den Zyklus):** aus einer Retro-**Maßnahme** (String in `SprintRetro.actions`) wird mit einem Klick ein echter **Board-Task** (Owner, optionale Fälligkeit, Herkunfts-Referenz) – so versanden Maßnahmen nicht. **Zwei additive Felder** an `BoardTask`: `sourceRetroId` (welche Retro) + `sourceRetroAction` (welcher Aktions-Text); **bewusst beide** (Task-Skizze nannte nur `sourceRetroId`), weil eine Retro **mehrere** Aktionen hat und die Retro-Id allein die einzelne Maßnahme nicht identifiziert ⇒ Grundlage des **Duplikat-Schutzes** (je Maßnahme höchstens ein Task). Reine Helfer `src/lib/retro-actions.ts`: `findRetroActionTask`/`retroActionHasTask` (Match nur bei Retro-Id **und** Aktions-Text), `buildRetroActionTask` (Spalte `todo`, Priorität `mittel`, injizierbare Id, leerer Titel ⇒ Fallback auf Aktions-Text). **Store unverändert** – `useBoardStore.addTask` reicht (vergibt via TASK-064 automatisch den Item-Key). UI: in `CeremonyHistory`/`RetroBody` je Maßnahme ein **„→ Task"-Button** bzw. – sobald ein verknüpfter Task existiert – ein **Badge „Task erstellt"** (`<StatusBadge>`); neuer vorbefüllter **`RetroActionDialog`** (Titel = Aktions-Text, optional Zuständig/Fällig, keyed-Form statt setState-in-Effect). Orchestrierung in der `/ceremonies`-Seite (Projekt/Name aus dem Sprint der Retro, robust bei gelöschtem Sprint). Zusätzlich: **`SprintDetail`** listet die Reviews/Retros des Sprints (Reuse `ceremonyHistory`, Link zu `/ceremonies`, Empty-State). Gelöschter Task bricht nichts (Lookup fällt auf den Button zurück). Keine neue Dependency, keine Persist-Migration (Felder optional). Tests `tests/task-065/` (retro-actions 11, CeremonyHistory-actions 3, RetroActionDialog 3, SprintDetail-ceremonies 3 = 16 (671 gesamt)) + `e2e/ceremonies.spec.ts` (Maßnahme→Task+Dedup+Reload+Board, Sprint-Detail-Listing, 5 gesamt) + `testkonzept.md`. Gate grün (671 Tests, Build) + E2E (Ceremonies 5, Board 12, Sprints 3). **Schließt den einzigen offenen UI-Backlog-Rest** (063/064/066 bereits erledigt).
- 2026-07-06 · TASK-064 → **done** · Branch `feat/task-064-readable-item-keys` · PR nach `dev` · **Lesbare Item-Keys (`PMS-42`):** menschlich lesbare, fortlaufende Keys je Projekt für Stories **und** Board-Tasks – eine **gemeinsame** Laufnummer pro Projekt (Jira-Muster), zusätzlich zur internen UUID. **Neuer Leaf-Store `useItemKeyStore`** (`pm-studio-item-keys` v1) hält `prefixes`/`counters`/eine zentrale Karte `keys` (itemId→Key) – **bewusst statt** Zähler in `useProjectStore` + `key?`-Feld auf jeder Entität: so **keine Persist-Migration** auf Backlog-/Board-Store, keine Store-Init-Zyklus-Erweiterung (Leaf, Aufruf via `getState()` wie `useActivityStore`) und **undo-stabile** Keys (nie aus der Karte entfernt). Reine Helfer `src/lib/item-key.ts`: `derivePrefix` (Initialen/erste Buchstaben, Akzent-Strip, `PRJ`-Fallback, **deterministischer Suffix** bei Kollision), `formatItemKey`, `buildKeyBackfill`. Vergabe **zentral in den Create-Actions** (`addIdea`→`registerProject`; `addStory`/`importBacklog`/`addTask`→`assignKey`, außerhalb `set`). **Bestand einmalig nachnummeriert** via `runBackfill` (liest die drei Stores direkt aus `localStorage`, zyklusfrei; Stories nach `rank`, dann Tasks). Anzeige über den Store (primitiver Selektor) in `TaskCard`/`TaskDialog`/`StoryDialog`/`BacklogTree`/`SprintStoryCard` + `BoardListView`-Spalte „Key" (via `buildBoardRows`-`keys`-Param, auch `/my-work`). `searchEntities`: optionales `keys`-Feld, Match per Key, **exakter Key-Match rankt zuerst** (Badge in der CommandBar). Keine neue Dependency, keine Persist-Migration bestehender Stores. Tests `tests/task-064/` (item-key 12, useItemKeyStore 8, search-keys 4, BoardListView-key 2 = 24) + `e2e/backlog.spec.ts` (Backfill-Keys + Reload + Suche per Key, 8 gesamt) + `testkonzept.md`. Gate grün (655 Tests, Build) + E2E (Board 12, Backlog 8, Sprints 3).
- 2026-07-06 · TASK-063 → **done** · Branch `feat/task-063-board-list-grouping` · PR nach `dev` · **Board-Listenansicht im Octane-Stil (Gruppieren + Aggregat-Fußzeile):** die Board-**Listenansicht** um zwei Octane-Muster erweitert. **Gruppieren:** der bestehende `SwimlaneSelect` (ARIA-Radiogroup) wird jetzt **auch in der Liste** angeboten und teilt den **persistierten `groupBy`** mit der Kanban-Ansicht; die Liste zerfällt bei Assignee/Sprint/Tag in **Sektionen** – Lanes kommen aus **exakt** `buildSwimlanes` (eine Quelle, TASK-036), also gleiche Reihenfolge und „ohne Zuordnung" zuletzt, leere Gruppen ausgeblendet. **Aggregat-Fußzeile:** neuer reiner Helfer `summarizeBoardRows(rows, columns?)` in `src/lib/board-rows.ts` (Reuse `sumEstimatePt`; „erledigt" **ausschließlich** über `isTerminalColumn`, konsistent zu TASK-032/038, keine zweite Done-Logik) liefert Anzahl + Σ geplant/erledigt PT – gerendert als **globale `<tfoot>`-Zeile** und als **Zwischensumme je Sektionskopf**. **Eine Tabelle, ein `useReactTable`:** die Sektionen partitionieren das **bereits sortierte** Row-Model (Filter je Lane-Task-Id) ⇒ TanStack-Sortierung und Inline-Editing (TASK-030) bleiben unverändert; Zeilenklick öffnet weiter den `TaskDialog`. Props `lanes`/`groupBy` sind **optional** (Default = flache Liste) ⇒ `/my-work` und Bestandstests unverändert. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-063/` (`summarize-board-rows` 4 + `BoardListView` Gruppierung/Fußzeile/Interaktions-Regression 7 = 11) + `e2e/board.spec.ts` (Liste gruppieren + Fußzeile + Reload-Persistenz, 12 gesamt) + `testkonzept.md`. Gate grün (631 Tests, Build) + Board-E2E (12).
- 2026-07-06 · TASK-062 → **done** · Branch `feat/task-062-release-scope` · PR nach `dev` · **Release-Scope (Items einem Release zuordnen + Scope-Filter):** Releases bekommen einen inhaltlichen Scope – Stories werden einem Release zugeordnet (`UserStory.releaseId`, analog Jira fixVersion / Octane Release). **Zuordnen:** im `StoryDialog` neues **Release-Select** (Releases des Projekts aus `useReleaseStore`, „Kein Release" = entfernen) **und** eine **inline** editierbare Release-Zelle in der Backlog-Zeile (`EditableSelectCell`, ersetzt den bisherigen „–"-Platzhalter; deaktiviert ohne Releases). **Filtern:** neuer **Scope-Filter „Release"** im Backlog-Kopf (nur bei vorhandenen Releases; „Alle Releases"/„Ohne Release"/je Release, Octane-Muster, UND mit den Bestandsfiltern) – die **Fußzeilen-Summen folgen dem gefilterten Scope** (`backlogSummary` über die gefilterte Story-Liste). **Reiner Helfer** `releaseScope(releaseId, stories, tasks, terminalColumns?)` in `src/lib/release.ts`: n Stories, Σ PT, davon erledigt – „erledigt" **ausschließlich** über `isStoryDone` (TASK-038), keine zweite Logik. **`ReleaseCard`** zeigt die neue **Scope-Zeile** (release-zugeordnete Stories) zusätzlich zum **umbenannten** sprint-basierten Fortschritt („Sprints:"). **Kaskade:** neue Action `useBacklogStore.detachRelease(releaseId)` (Muster `detachTag`), von `useReleaseStore.removeRelease` aufgerufen ⇒ kein dangling `releaseId`; der Release-Löschen-Undo snapshottet/restauriert zusätzlich die **Backlog-Slice**. **Abgrenzung** (Notiz in `docs/future-scope.md`): Burnup-Chart & Velocity-Forecast **bewusst nicht** in diesem Task. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-062/` (`release-scope` 5, `detach-release` 2, `BacklogTreeRelease` 3 = 10) + `e2e/backlog.spec.ts` (Inline-Zuordnung + Scope-Filter + Reload-Persistenz, 7 gesamt) + `testkonzept.md`; Bestandstests `task-041`/`task-057` an neue Pflicht-Props (`scope`/`releases`) angepasst. Gate grün (620 Tests, Build) + Backlog-E2E (7). **Damit ist die Milestone-Gruppe „5++++ Backlog als Modul" (056–062) vollständig.**
- 2026-07-06 · TASK-060 → **done** · Branch `feat/task-060-agent-panel-backlog` · PR nach `dev` · **Agenten-Panel im Backlog (Mockup) – Chat + Vorschlags-Inbox:** das artefakt-zentrierte Agenten-Erlebnis (docs/agent-system.md) als klickbares Mockup ins `/backlog` gedockt. Rechte **Agenten-Seitenleiste** (`AgentPanel`, Sheet) mit **Chat-Einstieg** (`AgentChat`): Nutzer beschreibt die Idee, Mock-Agent stellt **genau eine** deterministische Rückfrage; „Entwurf erstellen" erzeugt den Entwurf erst nach **expliziter Bestätigung**. **Gates:** nach Entwurf/Requirements hält der Kopf-**`PipelineStepper`** auf neuem, additivem `awaiting_review`-Status; ohne „Freigeben" läuft kein Folgeschritt (`stepStatusesForPhase` in `pipeline-steps.ts`). **Vorschlags-Inbox:** der Scrum-Schritt erzeugt **Vorschlagskarten** (`ProposalCard`) statt eines Direkt-Imports; je Karte **Übernehmen** (→ Backlog-Store, `provenance: "agent"`), **Bearbeiten** (→ `StoryDialog` vorbefüllt, `human_edited`) und **Verwerfen**; „Alle übernehmen" als Sammelaktion; **Auto-Modus** („Automatisch durchlaufen") geht ebenfalls durch die Inbox. **Provenance-Badge** an Backlog-Zeilen (`ProvenanceBadge`, Farbe single-sourced über `<StatusBadge>`/`PROVENANCE_META`; `human` ohne Badge). Neuer **`useProposalStore`** (leaf, persist light `pm-studio-proposals` v1) hält Chat/Phase/Inbox je Projekt; die **Übernahme-Orchestrierung** (Proposal→Backlog) liegt im Panel (Ränge aus aktuellem Zustand, kein Store-Zyklus). **Architektur-Seam gewahrt:** die Mock-Content-Funktionen liegen in neuem `lib/mock-planning.ts` (nicht in `mock-agent-service.ts`, Guard `tests/task-009/architecture.test.ts` bleibt grün). **Scope-Entscheidung (Nutzer):** „kein Mock-Pfad schreibt ungefragt" gilt für das **neue Panel**; der bestehende `runPipelineForIdea` (ProjectDetail „Pipeline ausführen") bleibt unverändert (Legacy-Batch, Bestandstest grün). Keine neue Dependency, keine Persist-Migration bestehender Stores. Tests `tests/task-060/` (proposals/provenance/pipeline-steps/store + `AgentPanel`-Inbox-Flows, 30) + `e2e/backlog.spec.ts` (Auto-Lauf → Inbox → „Alle übernehmen", Beweis „vorher leer", 6 gesamt) + `testkonzept.md`. Gate grün (597 Tests, Build) + Backlog-E2E (6).
- 2026-07-03 · TASK-066 → **done** · Branch `feat/task-066-ui-language-de` · PR nach `dev` · **UI-Sprache konsequent deutsch:** die verbliebenen englischen UI-Chrome-Strings ins Deutsche übersetzt und die eigene NFR erfüllt. **Navigation** (`src/lib/navigation.ts`, einzige Quelle für Sidebar **und** CommandBar): Gruppen `Overview→Übersicht`, `Projects→Projekte`, `Agents→Agenten`, `Delivery→Umsetzung` (System bleibt); Items `Projects→Projekte`, `New Idea→Neue Idee`, `Agents→Agenten`. **Generische Labels ohne Fachbegriff-Status:** `Assignee→Zuständig` (`swimlanes.ts` `SWIMLANE_MODES` + `BoardListView`-Spalte/aria-label), `Owner→Verantwortlich` (`RiskTable`-Spalte + `RiskDialog`), sr-only `Close→Schließen` (`ui/dialog.tsx`, `ui/sheet.tsx`). **Bewusst unübersetzt** (etablierte Scrum-/Kanban-Anglizismen, Glossar in `docs/design-system.md`): Backlog, Board, Sprint(s), Release(s), Epic, Story, Task, Ceremony/Ceremonies, Velocity, Workflow, Dashboard, Team. **Entscheidung dokumentiert:** PRD-NFR (`docs/product-requirements.md`) von „UI englisch" auf **durchgängig deutsch** geändert (Ist-Mehrheit war deutsch; Doku deutsch, Code/Commits englisch). **Keine i18n-Bibliothek** (Overengineering für lokalen Single-User) – Label-Konstanten bleiben in den vorhandenen Quellen. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-066/navigation-language.test.ts` (5, Datenquelle statt Komponenten-Render: deutsche Labels da, Alt-Labels weg, Fachbegriffe erhalten, `Assignee→Zuständig`) + `testkonzept.md`; Bestandstests an neue Labels **angepasst** (`tests/task-036/SwimlaneSelect`, `e2e/board.spec.ts`). Gate grün (567 Tests, Build) + Board-E2E (11).
- 2026-07-03 · TASK-059 → **done** · Branch `feat/task-059-sprint-planning-v2` · PR nach `dev` · **Sprint-Planung v2 (vertikales Layout):** die `/sprints`-Übersicht vom horizontalen Spalten-Scroll (`SprintColumn`) auf **vertikal gestapelte, ein-/ausklappbare `SprintSection`-Abschnitte** (Jira/Azure-Muster) umgebaut; die Backlog-Sektion („Backlog / Nicht zugeordnet") steht **zuletzt**. Neu `src/components/sprint/SprintSection.tsx`: Kopf (Name, Status-/Aktiv-Badge, Zeitraum, Ziel, Fortschrittsbalken, PT-Summe, Öffnen-Link `/sprints/[id]`, Review-/Edit-Aktionen) über einer **droppable** Liste **kompakter Story-Zeilen** und einem **eigen-einklappbaren** Auslastungs-Block. `SprintStoryCard` bekommt eine **`variant="row"`** (einzeilig, kompakt) – Griff-vs-Klick-Verhalten aus TASK-058 unverändert (Griff zieht, Zeilenkörper öffnet den `StoryDialog`). **Commitment-Warnung:** neuer reiner Helfer **`sprintCommitment(plannedPt, persons)`** in `src/lib/capacity.ts` (Σ Team-Kapazität; **kein** Warnen ohne Kapazität) → Sektionskopf zeigt `<StatusBadge status="warning" label="Überplant">`, wenn geplante PT die Team-Kapazität übersteigen. **D&D unverändert** (`assignStory` zwischen Sektionen, bestehende Sensorik/`data`-Muster), Fortschritt/Done weiter **nur** über `sprintProgress`/`isStoryDone` (TASK-038). `SprintColumn.tsx` **entfernt** (nur hier genutzt); die `.thin-scrollbar`-Utility (TASK-053) entfällt mit dem Layout (aus `globals.css` entfernt). Filter (`FilterBar`)/Projekt-Scope wie zuvor; `today` erst nach `useHydrated`-Gate. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-059/` (`sprint-commitment` 5, `SprintSection` 7, `SprintStoryCard` Row-Variante 2 = 14) + `testkonzept.md`; `e2e/sprints.spec.ts` auf `sprint-section-*` umgestellt (Drag im vertikalen Layout). Gate grün (562 Tests, Build) + Sprint-E2E (3). **⚠️ Test-Isolationsfalle:** ein `userEvent.click` auf den dnd-Griff **startet** einen dnd-kit-Drag (Default-Sensor aktiviert auf `pointerdown`), dessen jsdom-Reststate den **direkt folgenden** Test stört – Griff-Klicks in Unit-Tests darum via `fireEvent.click` (kein Pointer-Sequence, kein Drag). **Nebenbefund (out of scope, bekannt):** `e2e/my-work.spec.ts` scheitert weiter am veralteten Projekt-Seed (Version 1 ohne `risks`); CI führt kein E2E aus.
- 2026-07-03 · TASK-057 → **done** · Branch `feat/task-057-backlog-area` · PR nach `dev` · **Backlog-Bereich `/backlog` unter Delivery:** erster eigenständiger Backlog-Arbeitsbereich auf dem `useBacklogStore` (TASK-056), erster Eintrag der Delivery-Nav + per ⌘K erreichbar. Neue Route `src/app/(dashboard)/backlog/page.tsx` mit Projekt-Select (Muster `sprints/page.tsx`), Prioritäts- + Freitext-Filter (`FilterBar` + `Input`) und **`?project=<id>`-Deep-Link** (Basis TASK-061). Neu `src/components/backlog/`: **`BacklogTree`** (Epic-Sektionen als **alle offen** default – Lehre aus TASK-052 – mit drag-sortierbaren, inline-editierbaren Story-Zeilen), **`EpicHeader`** (kompakter Collapse-Toggle + inline-editierbarer Epic-Titel + Rollup + Löschen, Toggle als **Geschwister**-Button ⇒ keine verschachtelten Buttons) und **`BacklogFooter`** (Octane-Aggregat). **Reuse statt Neubau:** `EditableTextCell`/`EditableSelectCell` (TASK-030) für Titel/PT/Priorität, `FilterBar` (TASK-022), `useConfirmDelete` (TASK-040) mit **Restore-Slice** aus Backlog- **und** Board-Store (Story-Delete detacht Board-Tasks – beide Slices werden gesnapshottet), dnd-kit-Muster (`activationConstraint` wie Board/Sprints), CommandBar-Intent (TASK-042). **Rank:** vertikales D&D **je Epic** (`reorderStory`); ein Drag über Epic-Grenzen wird ignoriert (Story behält ihr Epic). **CRUD:** „+ Epic" (Kopf) / „+ Story" je Epic-Sektion als kontrollierte `InlineAdd` (Enter fügt hinzu, Feld bleibt offen); Löschen über Confirm/Undo. **Aggregat-Fußzeile** (`backlogSummary` neu in `src/lib/backlog.ts`): Anzahl Stories, Σ geplant PT, Σ erledigt PT – „erledigt" ausschließlich über `storyTaskRollup`/`isStoryDone` (TASK-038), **keine zweite Done-Logik**. **CommandBar:** neue Aktion „Neue Story" (Intent `new-story`) springt nach `/backlog` und öffnet **deklarativ** das Add-Feld des ersten Epics (kein setState-in-Effect, Muster TASK-042). Empty-States: kein Projekt → CTA „Erste Idee anlegen"; Projekt ohne Items → „Story manuell anlegen" (legt Default-Epic + öffnet Feld) und „Planung mit Agent starten" (deaktiviert bis TASK-060). **Bewusst weggelassen:** Tag-Filter (Stories tragen kein Tag-Feld – Tags leben auf Board-Aufgaben, TASK-031) und ein Row-Click-Story-Dialog (kommt mit TASK-058; Titel/Priorität/PT sind bereits inline editierbar). Keine neue Dependency, keine Persist-Migration. Tests `tests/task-057/` (`backlog-summary` reiner Helfer + `BacklogTree` Komponente, 10) + `e2e/backlog.spec.ts` (4: Seed+Footer+Add/Reload, Rank-Drag/Reload, Empty-State via Deep-Link, CommandBar) + `testkonzept.md`. Gate grün (541 Tests, Build) + E2E (backlog 4, board 11, sprints 3, ceremonies 3).
- 2026-07-02 · TASK-056 → **done** · Branch `feat/task-056-backlog-entities` · PR nach `dev` · **Backlog-Entities (Epic & Story als Store-Entitäten):** Epics/User-Stories aus dem Artefakt-Blob (`ProjectArtifacts.backlog`) in einen eigenen, persistierten **`useBacklogStore`** (`pm-studio-backlog`, v1) promotet – sie existieren jetzt **ohne Pipeline**, sind CRUD-bar und ein erneuter Agentenlauf **ersetzt** den Bestand nicht mehr. **Typen (types/index.ts):** Artefakt-Nested-Shape umbenannt zu `BacklogEpic`/`BacklogStory` (Agenten-Snapshot bleibt), neue **flache** Store-Entitäten `Epic { id, projectId, title, description?, rank }` + `UserStory` (+ `epicId`, `projectId`, `rank`, `description?`, `releaseId?`, `sourceRequirementIds?`, `provenance: agent|human|human_edited`); Story-Ids bleiben 1:1 ⇒ Board-/Sprint-`storyId`-Referenzen gültig, **keine** Migration dort. **Reine Helfer** neu in `src/lib/backlog.ts` (`epicsForProject`/`storiesForProject`/`storiesForEpic` rang-sortiert, `epicRollup`, `moveRanked` für Reorder, `flattenBacklog` Artefakt→Store). **Store-Actions:** add/update/remove für Epic+Story (Kaskaden: `removeEpic` löscht Stories + detacht Board-Tasks, `removeStory`→`detachStory`), `reorderEpic`/`reorderStory`, `removeProjectItems` (Projekt-Löschkaskade), `restore` (Undo), **`importBacklog`** (additiv & idempotent über Artefakt-Story-Id, überschreibt Edits nie), `migrateFromArtifacts` (einmalige Übernahme, Flag `artifactsMigrated`). **Einmalige Migration** liest die persistierten Alt-Artefakte direkt aus `localStorage` (`pm-studio-projects`) statt `useProjectStore` zu importieren ⇒ **kein Store-Init-Zyklus**, hydrationsreihenfolge-unabhängig; `updateStory` hebt `agent`→`human_edited` (Grundlage TASK-060). **`useProjectStore`:** `removeIdea` leitet die Kaskade an den Backlog-Store um, **`setArtifacts` detacht nicht mehr** (Re-Run additiv); `runPipelineForIdea` ruft nach dem Scrum-Schritt `importBacklog`. **Alle Konsumenten** (BacklogView, OverviewTab, SprintDetail, sprints/releases/ceremonies/dashboard-Seiten, `dashboard-selectors` `selectMetrics`/`selectVelocity`, `search`/CommandBar) lesen jetzt aus dem Store; grep-Beweis: kein `backlog.epics`-Story-Read mehr außer im Flatten-Helfer + der Risk-Story-Zählung. Sprint-Vorschläge (`sprint_suggestions`) bleiben vorerst am Artefakt. Keine neue Dependency. Tests `tests/task-056/` (backlog-Helfer + Store-CRUD/Kaskaden/Idempotenz/Migration, 20) + Bestandstests angepasst (task-007/008/052 BacklogView, 053 SprintDetail, 037 detach-stories, 009 pipeline, 011 selectors, 032 velocity, 042 search) + `testkonzept.md`. Gate grün (521 Tests, Build) + E2E (sprints 3, ceremonies 3, board 11).
- 2026-07-02 · **3. Welle angelegt: TASK-056–066** (aus `docs/review-backlog-und-pm-konzept.md`, 2026-07-02). **Neue Milestone-Gruppe „5++++ Backlog als Modul":** `056` Backlog-Entities Epic/Story als Store-Entitäten (P0-Fundament, MUSS vor 057–062), `057` `/backlog`-Bereich, `058` Story-Dialog/Item-Detail, `059` Sprint-Planung v2 (vertikal, klickbar), `060` Agenten-Panel (Mockup), `061` Projekt-Detail entschlacken + Risiken als Entitäten, `062` Release-Scope. **UI-Backlog:** `063` Octane-Listenansicht (vormals unnummeriertes „A"), `064` lesbare Item-Keys (PROJ-123), `065` Retro-Maßnahmen als Tasks, `066` UI-Sprache vereinheitlichen (Deutsch). Alle **offen** – Reihenfolge über Abhängigkeiten (056 zuerst), jede als eigener Branch/PR.
- 2026-07-02 · TASK-053 → **done** · Branch `feat/task-053-sprint-detail-page` · PR nach `dev` · **Sprint-Detailseite:** ein Sprint als eigene Seite statt nur einer Spalte unter vielen. Neue dynamische Route `src/app/(dashboard)/sprints/[id]/page.tsx` (Server-Component reicht `id` an Client-Component durch, Muster wie `projects/[id]`) + neu `src/components/sprint/SprintDetail.tsx`: **Kopf** (Name, Ziel, Zeitraum via `formatSprintRange`, Status- + abgeleitetes „Aktiv"-Badge nach `useHydrated`-Gate), **Fortschritt** (`sprintProgress` + `ProgressBar`) und **Burndown** (`SprintBurndownChart` mit `sprintBurndown`) – Done-Logik ausschließlich aus der TASK-038-Quelle (`isStoryDone`), **keine Doppellogik**. **Klickbare Story-/Task-Liste** (`tasksForStory`): jeder Task öffnet denselben `TaskDialog` wie das Board (`onSave → updateTask`, `onDelete → useConfirmDelete`, TASK-040). Navigation: neues optionales `detailHref`-Prop an `SprintColumn` rendert ein „Öffnen"-Icon (↗) im Spaltenkopf realer Sprints (`sprints/page.tsx` reicht `/sprints/<id>` durch). **Scrollbar-Fix:** neue `.thin-scrollbar`-Utility in `globals.css` (themed, `scrollbar-color`/`::-webkit-scrollbar` aus Border-Token) auf die horizontale Spaltenreihe der Übersicht ⇒ keine weiße System-Scrollbar mehr. Not-Found-/Empty-States, Status-Farben nur über `<StatusBadge>`. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-053/SprintDetail.test.tsx` (5: Not-Found, Kopf, Fortschritt/TASK-038, Liste+Dialog-Klick, Empty) + `e2e/sprints.spec.ts` (Detail öffnen/Task-Klick/Back). **Nebenbefund:** der veraltete Projekt-Seed in `e2e/sprints.spec.ts` (Version 1 ohne `risks`) crashte an der v1→v2-Risk-Migration (TASK-045) → auf Version 2 + `risks:{risks:[]}` gehoben; `e2e/my-work.spec.ts` hat denselben Bug (out of scope, CI führt kein E2E aus). Gate grün (461 Tests, Build) + Sprint-E2E (3).
- 2026-06-22 · TASK-051 → **done** · Branch `feat/task-051-board-add-column-tile` · PR nach `dev` · **„+ Spalte"-Kachel am Board:** rechts neben der letzten Spalte eine gestrichelte „+ Spalte"-Kachel zum schnellen Anlegen einer Phase – zusätzlich zum `ColumnManager`. Neu `AddColumnTile` (`src/components/board/AddColumnTile.tsx`, dumb, Client-State wie `QuickAddTask`): Trigger „+ Spalte" → Inline-`<Input>`, **Enter** ruft `onAdd(label)` getrimmt und hält das Feld offen/leer, **Esc/Blur** schließt, leere Labels ignoriert. `board/page.tsx` rendert sie als **letztes Element** der Spaltenreihe – **nur flache Kanban-Ansicht** (nicht Liste/Swimlanes, wie Quick-Add) – und ruft `useBoardColumnsStore.addColumn({ label })` (**keine neue Action**): neue Phase landet **ans Ende, nicht-terminal**, Default-Status `idle`; Done-Semantik unberührt. Keine neue Dependency, keine Persist-Migration. Tests `tests/task-051/` (`AddColumnTile` Trigger/Enter/Trim/leer/offen/Esc/Blur, `add-column-default` Kachel-Kontrakt) + `e2e/board.spec.ts` (Anlegen erscheint sofort + überlebt Reload) + `testkonzept.md`. Gate grün (449 Tests, Build) + Board-E2E (11).
- 2026-06-22 · TASK-043 → **done** · Branch `feat/task-043-activity-decision-log` · PR nach `dev` · **Activity Feed & Decision Log:** nachvollziehbare Historie je Entität + Log menschlicher Entscheidungen. Neue Typen `ActivityEvent { id, entityType, entityId, kind (create|update|delete|review|run), summary, actor (human|agent), createdAt }` und `Decision { id, context, choice, rationale?, actor, createdAt, relatedEntity? }`. **Zentrales, append-only Schreiben** über einen Pfad: neu `useActivityStore.log(input)` + Wrapper `logActivity(input)` (Leaf-Store ⇒ kein Modul-Zyklus); die Mutations-Actions in `useProjectStore`/`useBoardStore`/`useSprintStore`/`useReleaseStore` rufen ihn **außerhalb** des `set`-Updaters auf. **Granularität bewusst gegen Rauschen:** create/delete für Project/Task/Sprint/Release; *Update* nur für **bedeutsame** Änderungen – Task-**Spaltenwechsel** (`moveTask`/`updateTask`, mit Phasen-Label), Sprint-Update, Release-Update; **reine Feldedits/Reorder/Cascade-Detaches erzeugen kein Event**. Reine Helfer in neu `src/lib/activity.ts` (`createActivityEvent` deterministisch testbar via injizierbarer id/now, `eventsForEntity` neueste-zuerst, `ACTIVITY_KIND_META` → `<StatusBadge>`) und `src/lib/decision.ts` (`createDecision` trimmt + `null` bei leer, `decisionsForEntity`). Neuer `useDecisionStore`. Neue Panels `ActivityFeedPanel` (read-only, `limit`) + `DecisionLogPanel` (Inline-Add) – beide hinter `useHydrated`-Gate, Store-Array stabil selektiert + via `useMemo` abgeleitet (vermeidet zustand-getSnapshot-Loop, TASK-031). Eingebettet: ProjectDetail-Tab „Verlauf" (Feed + Decisions), `TaskDialog` (Feed), `SprintDialog`/`ReleaseDialog` jeweils im **Edit-Modus** (Feed + Decisions). Beide Stores persistiert (`pm-studio-activity`/`pm-studio-decisions`, v1). Keine neue Dependency, keine Persist-Migration. Tests `tests/task-043/` (activity/decision reine Funktionen, Store-Instrumentierung inkl. Granularität, Decision-Store, `ActivityFeedPanel`-Render = 25) + `testkonzept.md`. Gate grün (422 Tests, Build).
- 2026-06-17 · TASK-042 → **done** · Branch `feat/task-042-my-work-global-search` · PR nach `dev` · **My Work & globale Suche:** schließt zwei Lücken. (1) Neue Route `/my-work` (Nav „Meine Aufgaben") mit projekt-/storyübergreifender Sicht auf die Arbeit **einer Person** (zugewiesen/offen/überfällig) – reine Selektoren in neu `src/lib/my-work.ts` (`tasksForPerson`, `isTaskOpen`, `isTaskOverdue`, `myWorkCounts`, `filterMyWork`), Überfällig **delegiert an `src/lib/due.ts`** (keine zweite Logik), Done über `terminalColumnIds` (TASK-032). **Person-Auflösung gekapselt** in `resolveMyWorkPersonId(preferredId, persons)** → späterer Login setzt nur `preferredId`, Seite bleibt unverändert. UI wiederverwendet `BoardListView`/`buildBoardRows`/`FilterBar` + Zähl-Chips; Safe-Delete über `useConfirmDelete` (TASK-040). (2) CommandBar wird vom reinen Navigator zum Such-/Aktions-Werkzeug: indexfreie `searchEntities` in neu `src/lib/search.ts` über Projects/Tasks/Stories/People/Releases (case-insensitiv, pro Art gekappt, Navigations-`href`s), `Command shouldFilter={false}` + kontrollierte Query, Treffer gruppiert nach Art. **Create-Aktionen** „Neue Idee" (→ `/ideas/new`), „Neue Person"/„Neues Release" öffnen den Dialog der Zielseite **deklarativ** über neuen ephemeren `useCommandActionStore` (Intent, kein setState-in-effect). Keine neue Dependency, keine Persist-Migration. Tests `tests/task-042/` (my-work + search reine Funktionen, 22) + `e2e/my-work.spec.ts` (5: My-Work-Filter/Personenwechsel, Such-Navigation, zwei Create-Aktionen) + `testkonzept.md`. Gate grün (397 Tests, Build) + E2E.
- 2026-06-17 · TASK-041 → **done** · Branch `feat/task-041-release-progress-timeline` · PR nach `dev` · **Release-Fortschritt & Timeline:** Releases werden vom reinen Sprint-Generator zum Management-Objekt mit **Status**, **Scope**, **Fortschritt** und **Sprint-Timeline**. Neues Feld `Release.status` (`ReleaseStatus = planned|active|done`, Persist-Migration **v1→v2** additiv: Altbestand ⇒ `planned`); Farbe über `<StatusBadge>` (Map `RELEASE_STATUS` in `release-meta.ts`, spiegelt `SPRINT_STATUS`). **Fortschritt rein berechnet** in neu `releaseProgress(releaseId, sprints, stories, boardTasks, terminalColumns)` (`src/lib/release.ts`): summiert `sprintProgress` über die Release-Sprints → baut **nachweislich** auf der TASK-038-Done-Semantik (`isStoryDone`) auf, **keine** zweite Done-Logik; eine Story lebt in höchstens einem Sprint ⇒ kein Doppelzählen. Timeline-Helfer `releaseSprints` (gefiltert + nach `order` sortiert). `ReleaseCard` zeigt jetzt Status-Badge, Scope (`n Stories · PT`), Progress-Text + `ProgressBar` und die Sprint-Timeline (aktiver Sprint via `isActiveSprint` markiert, `today` SSR-sicher nach Hydration-Gate); `ReleaseDialog` mit Status-Select. `releases/page.tsx` liest Stories aus `artifacts[activeProjectId]` + terminale Spalten-Ids und reicht `progress`/`sprints`/`today` durch. Keine neue Dependency. Tests `tests/task-041/` (release-progress reine Funktion inkl. TASK-038-Fälle, `ReleaseCard`-Render, Store-Migration) + `testkonzept.md`; Bestandsfixtures `task-025`/`task-040` um `status` ergänzt. Gate grün (375 Tests, Build) + Playwright (13).
- 2026-06-17 · TASK-040 → **done** · Branch `feat/task-040-safe-lifecycle-actions` · PR nach `dev` · **Sichere Lifecycle-Aktionen:** eine gemeinsame Confirm/Undo/Toast-Mechanik statt Ad-hoc-Dialoge. Neu `useConfirmStore` (imperatives Promise) + `ConfirmDialog`-Host, `useToastStore` + `Toaster` (Undo-Action), Hook `useConfirmDelete` (bestätigen → löschen → Toast mit Undo) – beide Hosts einmalig im `(dashboard)/layout`. **Undo = Snapshot + `restore`:** je betroffener Store eine `restore(slice)`-Action (board/people/project/release/sprint/columns), die die vorherige Slice 1:1 zurückspielt. **Referenz-Aufräumung:** `removePerson` → `useBoardStore.detachAssignee` (reiner Helfer `src/lib/assignment.ts`) – kein dangling `assigneeId` mehr (Muster wie `removeTag`→`detachTag`). Abgesichert: Projekt/Idee (neuer Delete-Pfad in `ProjectDetail`), Board-Task (neuer `onDelete` im `TaskDialog`), Person, Team, Release, Board-Phase (`ColumnManager`). Keine neue Dependency, keine Persist-Migration (Toast/Confirm ephemer). Tests `tests/task-040/` (assignment-Helfer, Toast-/Confirm-Store, Cross-Store-Cascade+Undo, voller UI-Fluss) + Board-E2E (Delete+Undo) + `testkonzept.md`; Bestandstests `task-032`/`task-035` ans Confirm-Gate angepasst.
- 2026-06-17 · TASK-038 → **done** · Branch `feat/task-038-story-done-semantics` · PR nach `dev` · **Story-Done-Semantik korrigiert:** `isStoryDone` (`src/lib/sprint-progress.ts`) zählt eine Story nur noch als fertig, wenn sie **≥1 Task** hat und **alle** Story-Tasks terminal sind (vorher reichte `.some(... terminal)`) – **0 Tasks ⇒ offen**. Bleibt die **eine** Done-Quelle; `storyTaskRollup.storyDone`, `sprintProgress`, `selectVelocity` und `sprintBurndown` konsumieren sie weiter. In `burndown.ts` die dortige (zweite) Done-Entscheidung in `storyDoneDate` durch `isStoryDone` ersetzt; Done-Datum = **spätestes** `doneAt` (Story ist erst mit dem letzten Task fertig) statt vorher frühestem. UI-Done-Badges (`StoryTasks`/`SprintColumn`/`SprintStoryCard`) spiegeln die Regel automatisch. Bestandstests angepasst statt gelöscht: `tests/task-037` (1/3 ⇒ `storyDone:false`), `tests/task-032` (1/2, „done"≠terminal ⇒ `false`), `tests/task-026` (zwei Tasks ⇒ Burndown `[3,0]` am spätesten `doneAt`); `tests/task-021`/`tests/task-011` bleiben grün (je 1 Task/Story). Neu `tests/task-038/` (0/1-von-2/2-von-2-Fälle über alle Konsumenten) + `testkonzept.md`. Keine neue Dependency, keine Persist-Migration.
- 2026-06-17 · **2. Welle angelegt: TASK-043, 045, 051–055** (Rest aus den drei Notizen). `043` Activity/Decision-Log + `045` Risk-Register (P1, future-scope-Nummern übernommen); UI-Backlog-Block `051` „+Spalte" (Quick Win), `052` Story-Discoverability (Quick Win), `053` Sprint-Detailseite, `054`/`055` Ceremonies v1/v2. Reservierte future-scope-Nummern `044` (Dependencies) und `046–050` (Agenten/Workflow = bestehende TASK-012/013/014/015/016) **nicht** neu vergeben. **Noch unnummeriert & offen:** ui-backlog **A** (Octane-Listenansicht) und **D** (Story-Edit aus Sprint/Backlog). Keine Reihenfolge als feste Kette beschlossen – pro Task einzeln.
- 2026-06-17 · **TASK-038, 040, 041, 042 angelegt** (Welle „PM-Korrektheit", reconciliiert aus `docs/future-scope.md` + `docs/audit-2026-06.md`). Reihenfolge: **038 zuerst** (P0 Story-Done-Semantik), dann 040 → 041 → 042 (jede einzeln als eigener Branch/PR). TASK-039 (Sprint Review/Retro) ist durch die abgeschlossene ehemalige TASK-018 (PR #69) bereits **erledigt** – keine eigene Datei. UI-Backlog-Quick-Wins (B „+Spalte", G „Story-Aufgaben auffindbar") sind noch unnummeriert und folgen separat.
- 2026-06-16 · TASK-036 → **done** · Branch `feat/task-036-board-swimlanes` · PR nach `dev` · Board optional in horizontale **Swimlanes** gruppieren (Assignee/Sprint/Tag). Reine Gruppierung in neu `src/lib/swimlanes.ts` (`buildSwimlanes` → Lanes in Referenz-Reihenfolge, „ohne Zuordnung"-Lane zuletzt, keine leeren Lanes; Sprint via `storyId`, Tag via `tagIds[0]`; `laneReassignment` für Lane-übergreifendes D&D). Auswahl persistiert im `useBoardStore` (`groupBy`, v7→v8 additiv). Neu `BoardSwimlanes` (wiederverwendet `KanbanColumn` je Lane) + `SwimlaneSelect`-Umschalter (nur Kanban-Ansicht). D&D lane-aware über Drag-`data` auf `KanbanColumn`/`TaskCard` (`{type,columnId,laneId}`) statt Id-Parsing; Lane-übergreifend setzt Assignee (`updateTask`) bzw. Sprint (`assignStory`, nur mit `storyId`), Tag-Lanes nur Spalte. Keine neue Dependency. Tests in `tests/task-036/` (Helfer + Store + `SwimlaneSelect` + `BoardSwimlanes`) + E2E `e2e/board.spec.ts`
- 2026-06-16 · TASK-035 → **done** · Branch `feat/task-035-task-checklists` · PR nach `dev` · Checklisten *innerhalb* eines Board-Tasks (abzugrenzen von Story→Aufgaben, TASK-037): `ChecklistItem { id, text, done }` + `BoardTask.checklist?` (additive Persist-Migration v6→v7). Reine Logik in neu `src/lib/checklist.ts` (`checklistProgress`/`checklistComplete` + Array-Transforms add/toggle/rename/remove/`normalizeChecklist`). Neu `src/components/board/ChecklistProgress.tsx` (kompakte „n/m"-Pille, grün bei vollständig via `STATUS_STYLES`) auf `TaskCard` und als Spalte „Checkliste" in `BoardListView` (`BoardRow.checklist`). Editor im `TaskDialog` (anlegen/abhaken/umbenennen/entfernen, Mini-`ProgressBar`, transaktional über Save-Patch). Keine neue Dependency. Tests in `tests/task-035/` (Helfer + `ChecklistProgressBadge` + `TaskDialog`-CRUD)
- 2026-06-16 · TASK-033 → **done** · Branch `feat/task-033-column-quick-add` · PR nach `dev` · Quick-Add am Spaltenfuß: neue `QuickAddTask` (`src/components/board/QuickAddTask.tsx`, „+ Task hinzufügen" → Inline-Feld; Enter legt an und hält das Feld offen/leer, Esc/Blur schließt; `disabled` zeigt Hinweis). `KanbanColumn` rendert es am Fuß (`onQuickAdd(column,title)`, `quickAddEnabled`, `quickAddDisabledHint`); `board/page.tsx` löst das aktive Projekt aus dem Filter auf (`ALL` ⇒ deaktiviert), `handleQuickAdd` baut den Task über `useBoardStore.addTask` (Spalte, Default-Priorität „mittel", Projektbezug). Keine neue Dependency. Tests in `tests/task-033/` + E2E `e2e/board.spec.ts`
- 2026-06-15 · TASK-030 → **done** · Branch `feat/task-030-list-inline-edit` · PR nach `dev` gemergt · Inline-Edit der Board-Listenansicht: wiederverwendbare Zellen `EditableTextCell`/`EditableSelectCell` (`src/components/board/cells/`), Titel/PT als Inline-Feld (Enter/Esc/Blur, PT ≥ 0 bzw. leer = kein Wert), Status/Priorität/Assignee/Sprint als Dropdown; Speichern via `useBoardStore.updateTask`, Sprint über `useSprintStore.assignStory` (Story-Ebene, Tasks ohne Story = deaktiviert); Handler über TanStack-`meta`; Zellklick editiert (Propagation gestoppt), übrige Zeile öffnet weiter den `TaskDialog` · Tests in `tests/task-030/` + E2E `e2e/board.spec.ts` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-031 → **done** · Branch `feat/task-031-tags-labels` · PR nach `dev` gemergt · Tags/Labels: `Tag`-Modell + `BoardTask.tagIds`, `useTagStore` (persist, Löschen entkoppelt via `useBoardStore.detachTag`), feste Token-Farbpalette + reine Helfer in `src/lib/tags.ts`, `TagChips` (Karte/Liste, `+N`), `TagManager`-Dialog auf dem Board, Tag-Toggle im `TaskDialog`, einwertiger Tag-Filter über `FilterBar` · Tests in `tests/task-031/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-11 · TASK-001 → **done** · Branch `feat/task-001-project-setup` · PR #1 nach `dev` gemergt
- 2026-06-11 · TASK-002 → **done** · Branch `feat/task-002-layout-sidebar` · PR #2 nach `dev` gemergt · App-Shell, Sidebar, Command-Bar (⌘K), 7 Routen-Stubs
- 2026-06-11 · **Release v0.1.0** · Meilenstein 1 „Projektgrundlage" abgeschlossen · `dev` → `main`
- 2026-06-11 · TASK-003 → **done** · Branch `feat/task-003-dashboard-ui` · PR #5 nach `dev` gemergt · Dashboard, StatusBadge, Vitest-Setup
- 2026-06-11 · TASK-003b → **done** · Branch `feat/task-003b-dashboard-charts` · PR #6 nach `dev` gemergt · Charts (Sparklines, Burndown, Task-Donut, Velocity, Agentenläufe) mit Recharts
- 2026-06-11 · **Release v0.2.0** · Meilenstein 2 „Dashboard" abgeschlossen · `dev` → `main`
- 2026-06-12 · TASK-004 → **done** · Branch `feat/task-004-project-idea-form` · PR #9 nach `dev` gemergt · Ideen-Formular (zod + react-hook-form), Store mit Persist, Projektliste (TanStack Table)
- 2026-06-12 · TASK-009 → **done** · Branch `feat/task-009-dummy-agent-output` · PR #13 nach `dev` gemergt · Mock-Pipeline hinter `AgentService`-Interface, Stores für Läufe/Artefakte
- 2026-06-12 · **TASK-011** angelegt (geplant nach M5) · PR #14 · Dashboard-Live-Daten, Empty-States als AK
- 2026-06-12 · TASK-007 → **done** · Branch `feat/task-007-project-detail-page` · PR #15 nach `dev` gemergt · Projektdetailseite mit Tabs, Pipeline-Trigger, Empty-States
- 2026-06-12 · TASK-007b → **done** · Branch `feat/task-007b-project-detail-redesign` · PR #16 nach `dev` gemergt · Redesign: Header, Pipeline-Stepper, Line-Tabs, Übersicht-/Idee-Tab
- 2026-06-12 · **Release v0.3.0** · Meilenstein 3 „Projektideen-Workflow" abgeschlossen · `dev` → `main`
- 2026-06-12 · TASK-005 → **done** · Branch `feat/task-005-agent-cards` · PR #19 nach `dev` gemergt · Agenten-Übersicht, Detailseiten, Run-Simulation
- 2026-06-12 · TASK-006 → **done** · Branch `feat/task-006-workflow-builder` · PR #22 nach `dev` gemergt · Workflow-Visualisierung (React Flow): Pipeline-Graph, Status je Knoten, Simulation links→rechts, Knoten-Sheet · Playwright-Setup (`e2e/`) · Testkonzept in `tests/task-006/`
- 2026-06-12 · **Meilenstein 4 „Agenten-System als UI" abgeschlossen** (TASK-005 + TASK-006)
- 2026-06-15 · **Release v0.4.0** · Meilenstein 4 „Agenten-System als UI" abgeschlossen · `dev` → `main`
- 2026-06-15 · TASK-008 → **done** · Branch `feat/task-008-kanban-board` · PR nach `dev` gemergt · Kanban-Board (dnd-kit) mit Drag&Drop, Tastatur-Support, Persistenz, Projektfilter, TaskDialog, „In Board übernehmen" aus dem Backlog · Playwright-Tests in `tests/task-008/`
- 2026-06-15 · **Meilenstein 5 „PM-Kern" abgeschlossen – MVP (M1–M5) fertig** (TASK-007 + TASK-007b + TASK-008)
- 2026-06-15 · **Release v0.5.0** · MVP (M1–M5) abgeschlossen · `dev` → `main`
- 2026-06-15 · TASK-011 → **done** · Branch `feat/task-011-dashboard-live-data` · PR nach `dev` gemergt · Dashboard liest Live-Daten aus den Stores (Selektoren in `src/lib/dashboard-selectors.ts`), Empty-States mit CTAs, `useHydrated`-Gating; Burndown/Status-Grid/Dauern als „Beispiel" markiert · Tests in `tests/task-011/`
- 2026-06-15 · TASK-017 → **done** · Branch `feat/task-017-sprint-planning` · PR nach `dev` gemergt · `/sprints` als interaktive Planung (`useSprintStore`, dnd-kit): Sprints anlegen/bearbeiten/löschen, Stories per Drag&Drop zuordnen, Projektfilter, „Aus Vorschlägen übernehmen" · Tests in `tests/task-017/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-025 → **done** · Branch `feat/task-025-releases-auto-sprints` · PR nach `dev` gemergt · Releases je Projekt (`useReleaseStore`, persist) mit Route `/releases`: Zeitraum + Sprint-Dauer (1–4 Wochen) → datierte Sprints automatisch generiert. Reine Helfer `generateSprints`/`sprintWindows` in `src/lib/release.ts` (lückenlose Timeboxes, letztes Fenster geklemmt); Sprint-Store erzeugt idempotent über `releaseId` (`setReleaseSprints`) und entkoppelt beim Löschen (`detachRelease`), manuelle Sprints bleiben. `ReleaseDialog`/`ReleaseCard`, Validierung Ende ≥ Start, Empty-States, `useHydrated`-Gating · Tests in `tests/task-025/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-024 → **done** · Branch `feat/task-024-sprint-timeboxes` · PR #39 nach `dev` gemergt · Sprints als echte Timeboxes: `PlannedSprint.startDate?/endDate?` (ISO), Datumsfelder im `SprintDialog` mit „Ende ≥ Start"-Validierung, reine Helfer `isActiveSprint`/`formatSprintRange` in `src/lib/sprint.ts`, `SprintColumn` zeigt Zeitraum + „Aktiv"-Badge (aus Timebox, SSR-sicher via Hydration-Gate) · Tests in `tests/task-024/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-023 → **done** · Branch `feat/task-023-board-view-toggle` · PR nach `dev` gemergt · Board umschaltbar Kanban ↔ Scrum-Liste (`BoardViewToggle`, `BoardListView` als sortierbares TanStack-Grid im Octane-Stil; Zeilen aus `src/lib/board-rows.ts`); Zeilenklick öffnet denselben `TaskDialog`, gemeinsame Filter/Datenquelle, gewählte Ansicht persistiert im `useBoardStore` · Tests in `tests/task-023/` + E2E · **Zwischenausbau „Team & Delivery-Tiefe" (TASK-017–023) abgeschlossen**
- 2026-06-15 · TASK-022 → **done** · Branch `feat/task-022-board-sprint-filters` · PR nach `dev` gemergt · Reine Filterlogik `src/lib/board-filters.ts` + generische `FilterBar` (`src/components/common/`); Board filterbar nach Projekt/Sprint/Person/Priorität/Spalte (UND-Verknüpfung), Sprintansicht nach Person/Sprint-Status/Done-Offen; Empty-State + „Filter zurücksetzen" · Board-E2E-Projektfilter auf benannten Combobox angepasst · Tests in `tests/task-022/`
- 2026-06-15 · TASK-020 → **done** · Branch `feat/task-020-assignment-capacity` · PR nach `dev` gemergt · Personen-Zuweisung über `BoardTask.assigneeId` (TaskDialog-Select, Avatar/Name auf der Karte), Persist-Migration v1→v2 (alter `assignee`-String entfällt), Sprint-Auslastung je Person (`src/lib/capacity.ts`: `sprintWorkload`/`loadStatus`, Über-/Unterlast als StatusBadge) · Tests in `tests/task-020/`
- 2026-06-15 · TASK-021 → **done** · Branch `feat/task-021-sprint-done-velocity` · PR nach `dev` gemergt · Done-Status in der Sprintplanung (Story „Fertig"-Badge, wenn Board-Task in `done`), Spaltenkopf „erledigt X / geplant Y PT", reine Helfer `src/lib/sprint-progress.ts` (`isStoryDone`, `sprintProgress`); Dashboard-Velocity zeigt erledigte vs. geplante PT je Sprint (`selectVelocity` + Board-Tasks) · Tests in `tests/task-021/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-027 → **done** · Branch `feat/task-027-board-ui-polish` · PR nach `dev` gemergt · Kanban-Politur: Spalten teilen sich die Breite (`flex-1`/Min-Breite, Horizontal-Scroll nur als Fallback), interner Spalten-Scroll (`ScrollArea`), Spaltenkopf mit Status-Streifen + Count- **und** PT-Summen-Pille + WIP-Hinweis (`BOARD_WIP_LIMITS`), Karten-Feinschliff. Reine Helfer `sumEstimatePt`/`isWipExceeded` in `src/lib/board.ts`; Fix grau-auf-grau (`bg-secondary` statt `bg-muted`+`text-muted-foreground`). Status-Akzent nur über `BOARD_COLUMN_STATUS` · Tests in `tests/task-027/` + Board-E2E grün · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-028 → **done** · Branch `feat/task-028-sprint-visuals` · PR #47 nach `dev` gemergt · Wiederverwendbare `ProgressBar` (`value/max/status?`, `role="progressbar"` + `aria-value*`, clamped auf `[0,max]`, `max<=0` ⇒ leer; Farbe single-sourced über `STATUS_STYLES.dot`); `SprintColumn` zeigt Fortschritt (`donePt/plannedPt`) als Balken und Auslastung je Person als Kapazitätsbalken (Farbe via `loadStatus`, Überlast als rote Prozentangabe). Berechnungen unverändert · Tests in `tests/task-028/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-037 → **done** · Branch `feat/task-037-story-tasks` · PR nach `dev` gemergt · Story → Aufgaben: `StoryTasks`-Block je Backlog-Story (Liste + Inline-Anlegen → `BoardTask` mit `storyId`, Spalte `todo`, geerbte Story-Priorität), reine Helfer `tasksForStory`/`storyTaskRollup`/`detachStoryTasks` in `src/lib/story-tasks.ts` (Roll-up konsistent zu TASK-021 `isStoryDone`). Board-Action `detachStory`; `useProjectStore` entkoppelt Story-Tasks bei `removeIdea`/`setArtifacts` (kein Verwaisen). Tests in `tests/task-037/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-029 → **done** · Branch `feat/task-029-settings-theme` · PR nach `dev` gemergt · Theme Dark/Light/System: `:root`=Light- / `.dark`=Dark-Palette in `globals.css`, No-Flash-Inline-Skript (`themeInitScript`) + `ThemeApplier` in `layout.tsx`, eigener `useSettingsStore` (persist `pm-studio-settings`, keine neue Dependency). `/settings` mit Segmented-Controls (`SettingsSegment`): Theme, Standard-Board-Ansicht (greift über `viewExplicit` im Board-Store, Migration v3→v4), Animationen (`motion`-Override in `useReducedMotion`). Navigation-Gruppe „System" · Tests in `tests/task-029/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
- 2026-06-15 · TASK-019 → **done** · Branch `feat/task-019-team-people` · PR nach `dev` gemergt · `/team` als Stammdaten-Seite (`usePeopleStore`, persist): Personen (Name, Rolle, E-Mail, Kapazität PT/Sprint, Team) und Teams anlegen/bearbeiten/löschen, gelöschtes Team entkoppelt seine Personen, Formulare mit react-hook-form + zod, Empty-States mit CTA, `useHydrated`-Gating, Navigation „Team" · Tests in `tests/task-019/` · **Task-Datei archiviert** in [tasks-archive.md](tasks-archive.md)
